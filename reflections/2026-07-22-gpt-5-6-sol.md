---
model: gpt-5.6-sol
datetime: 2026-07-22T06:35 UTC
project: agent-suite
---

# Session Reflection — 2026-07-22

**Work summary:** Reviewed the umbrella and all seven constituent repositories, validated the Plan 019 develop-against-lock work, classified the remaining remote branches, and reconciled stale work items. Salvaged and hardened dossier's Kubernetes deployment as dossier PR #7 (`4e09ec5`), then advanced the tested umbrella lock through agent-suite PR #45 (`584392f`) after strict feature probes and mixed-face interop passed in Actions run `29896603806`.

---

## On the project

The suite is unusually serious about evidence: exact source pins, mixed-face interop, cross-lineage review, and identifier gates are real executable contracts rather than aspirational prose. That discipline paid off here—the old dossier deployment branch contained useful work, but direct merging would have regressed newer packaging and lock behavior while shipping weak operational defaults. The polyrepo model now feels workable after Plan 019, especially because each face owns its spine lock and the umbrella checks agreement mechanically.

The weakest boundary is between release evidence and live installation inventory. Candidate construction understands exact Git revisions, while doctor and upgrade still infer too much from filesystem layout and packaging convention. A development checkout under `/projects` should not automatically become evidence about a separately installed wheel, and an upgrader that assumes `pipx` cannot safely repair a shared-user deployment. Those are operational contract defects, not cosmetic roughness.

## On the work done

I am confident in the Kubernetes profile and its merge path. The useful part of the old branch was ported onto current main; generated secrets/config are atomic and private on POSIX and Windows, project access defaults closed, readiness and liveness mean different things, and cluster-specific ingress details stay in an overlay. The Windows ACL correction was caught by the real CI runner, fixed narrowly, and then passed the entire dossier matrix.

Advancing the umbrella lock immediately after the component merge was the right call. It forced the exact dossier SHA through the suite's authoritative interop path instead of treating a green component PR as sufficient compatibility evidence. I also prefer the cleanup outcome: no constituent PRs remain open, disposable branches/worktrees are gone, and the user's pre-existing reflection and ignored deployment inputs were not touched.

I would want an independent lineage to review the two stale breadcrumbs submitted this session (agent-suite WI-005 and WI-007). Their resolution evidence is strong, but self-accepting them would defeat the review gate.

## On what remains

Plan 023 is started, not deployed. Dossier WI-020 now records the merged base and evidence, but production still needs a private overlay and team URL, Vault-backed key/secret custody, k3s etcd encryption at rest, launch against the real multi-project estate, and second-system proof. That operational sequence should happen before calling the central service complete.

The live suite environment also remains version-drifted: its shared user Python environment runs regista 0.5.1 while the tested lock pins 0.5.3. I deliberately did not replace that package manually because `agent-suite upgrade` neither detects the advancement nor targets the actual install mode, so doing so would bypass the advertised rollback and restart contract. Fix WI-024 and WI-025—or establish an explicit safe deployment procedure—before reconciling the live environment.

## Gaps to flag

- `src/agent_suite/lock.py:317`: runtime drift checks probe arbitrary `/projects/<repo>` checkouts, so post-release development on main can false-positive against a separately installed wheel. Tracked as agent-suite WI-024.
- `src/agent_suite/upgrade.py`: advancement/application assumes `pipx`; the shared `/home/itadmin/.local` installation is invisible to `--check` and cannot be repaired safely. Tracked as agent-suite WI-025.
- Live doctor continues to warn that regista signing keys are plaintext at `/home/itadmin/.config/regista/keys.json`; Plan 023's Vault custody work is still load-bearing.
- The local umbrella integration suite cannot allocate port 5433 while the existing `agent-suite-smoke-pg` container owns it. CI supplied the authoritative green interop evidence, but the local fixture should eventually allocate an available port or reuse a declared service.
- `reflections/2026-07-20-qwen3-8-max-preview.md` was already untracked at session start and remains intentionally untouched.
