# 0A-2/3/5 round 2 — reviewer 3 DELTA re-review (minimax-m3, third lineage, 2026-08-25; stands in for Opus, which hit its spend limit)

# Phase 0A Delta Re-Review — reviewer 3 (third lineage, ollama-cloud/minimax-m3)
**Scope:** 0A-2 (`MECHANISM-TAXONOMY.md` + new `REPRODUCTIONS.md`), 0A-3 (`MATRIX-SCHEMA.md` + `MATRIX.csv`), 0A-5 (`BAKEOFF-RUBRIC.md`). Cross-checked against `TRUST-MODEL.md` v0.3 (accepted by reviewer 3 in the prior round) and the plan charter (`README.md`).
**Method:** every closure claim on B1–B18 / NB-1..NB-12 re-read against the cited line in the new artifacts. The new material (`REPRODUCTIONS.md`, deferred-profile elimination, owner/scope changes, work-load row additions, security-core split) attacked on its own terms. No gate transition recorded (text-only per the prompt).
---
## Closure table for B1–B18 and NB-1..NB-12
| Opus item | Status | Evidence (read, not trusted) |
|---|---|---|
| **B1** `cli-3` owner | **CLOSED** | `MATRIX.csv:65` now `cli-3,...,app:regista-cli,...` with note *"not assigned to kernel"*. `MECHANISM-TAXONOMY.md:74` re-worded to *"Owning boundary: app:regista-cli. Findings: `cli-3`."* `MATRIX-SCHEMA.md:22` enum now lists `app:regista-cli`. All three places reconcile. |
| **B2** taxonomy↔matrix ownership contradictions | **CLOSED** | Re-derived: `transition-role-default-deny` declares `gate-engine` (`MECH-TAX:82`); matrix gives `cairn-15` `gate-engine` (`:75`), `as-14`/`an-4` `gate-engine` (`:84`,`:89`). All three match. `typed-gate-result-and-exit-semantics` declares `bootstrap-root + broker` (`MECH-TAX:86`); matrix gives `as-3`/`as-16` `bootstrap-root` (`:80`,`:85`) and `acb-6` `broker` (`:97`). All three match. |
| **B3** `persist-9` home | **CLOSED (substantively; with a recorded drift)** | `MECH-TAX:23` now hosts `persist-9` under `unique-chain-head-binding`, boundary `kernel`, with note *"validly signed fork is owned by INV-005 chain uniqueness, not row/envelope reconciliation."* This is the correct home (TM:180 cites `persist-9` under INV-005). The `aw-7` INV-004 divergence is recorded at `MECH-TAX:25`. Both deltas are explicit and auditable. |
| **B4** probe falsifiability / oracle | **CLOSED** | Every one of the 78 `REPRODUCTIONS.md` rows now has columns: `Fixture / preconditions`, `Exact probe steps`, ``confirmed`` observable`, ``invalidated`` observable`, `Severity-carrying observation`, `Blank result block`. Discriminator between `not-reproducible` and `invalidated` is stated at the top (`:13`). |
| **B5** `crypto-4` conflation | **CLOSED** | `REPRODUCTIONS.md:93` separates exposure (blob appears in argv/output) from severity-confirming (adversary machine principal decrypts and uses key). The `Severity-carrying observation` column explicitly distinguishes the two. `as-5` (`:99`) likewise distinguishes *marker/material* from *reference/handle* presence. |
| **B6** High-bearing definition | **CLOSED (with stated `OPEN:` to owner)** | `MECH-TAX:10` now defines: *"**High-bearing** means a mechanism whose maximum effective severity is `High` or `Critical`, i.e. effective severity `>= High`; it includes Critical-only mechanisms even when they contain no finding literally rated High. `OPEN:` The owner must confirm this interpretation of the charter before probes are commissioned."* Six↔four-level mapping at `:14`. |
| **B7** `REPRODUCTIONS.md` exists | **CLOSED (file artifact created; executors not yet run)** | New `plans/025-0a/REPRODUCTIONS.md` exists with the 78-row table, pre-selection digest `sha256:5918…0651` recorded at `:5`, verdict contract at `:7–14`, executor-completion note at `:103–105`. As Opus noted in N1/B7: the file is the deliverable; the probes themselves and the verdicts are 0A-2 executor work, not the drafter's. Status is "EXECUTOR HAND-OFF" (`:1`) and the file ends explicitly with *"Until those actions occur, the charter criteria for attempted Criticals/High-bearing mechanisms, rerunnable probes, reviewed verdicts, and inventory `verified` updates remain unmet."* That honest scope is the right framing. |
| **B8** schema enum / kernel-sub-boundary collapse | **CLOSED (per-row; partial on sub-boundary granularity)** | Enum at `MATRIX-SCHEMA.md:22` adds `app:regista-cli` and `app:cairn`. All 147 matrix rows have a non-empty `owning_boundary`. The "ten kernel sub-boundaries collapse to `kernel`" complaint is partially addressed: schema rule 13 (`:46`) requires every row's `owning_boundary` and leading `sub_boundary=<slug>` to be derivable from its taxonomy mechanism entry, and the notes column carries the sub-boundary slug (e.g. `sub_boundary=kernel-first-write`, `sub_boundary=capability-manifest`). So the per-row accountability is preserved at the notes level. Whether this is sufficient for the original auditability demand is debatable — see NB-1 below. |
| **B9** 30 rows `deferred-profile` on unverified premise | **CLOSED** | `MATRIX.csv` contains **zero** rows with `deferred-profile` (grep count: 0). Schema rule 5 (`MATRIX-SCHEMA.md:38`) keeps `deferred-profile` as a valid value but with strict per-finding requirements. Schema ASSUMPTION (`:63`) says *"Existing acb and agent-wake developer-host paths remain reachable and therefore use `partially-mitigated` with broker/transport residual ownership; future truly absent surfaces may use `deferred-profile` only under rule 5."* All 16 acb rows and 14 agent-wake rows are now `partially-mitigated` with `residual_owner=` set in notes. `acb-1` (Critical) and `aw-7` (High) — the two rows Opus specifically flagged — both carry explicit `residual_owner=broker` / `residual_owner=transport` notes (`:27`,`:26`). |
| **B10** five owner↔invariant-home contradictions | **CLOSED** | Spot-checked all five: `SEC-05` (`:61`), `as-13` (`:83`), `persist-13` (`:70`) → `bootstrap-root` with `residual_owner=kernel` matching INV-009 home (`TM:184`). `acb-9` (`:29`) → `bootstrap-root` matching INV-035 home (`TM:210`). `dossier-14` (`:79`) → `bootstrap-root` matching INV-036 home (`TM:211`). `cairn-15` (`:75`) → `gate-engine` matching INV-024 home (`TM:199`). All consistent. |
| **B11** `an-5` split from `an-6`/`an-9` | **CLOSED** | `MATRIX.csv:52` now `an-5,...,app:agent-notes,...,Phase 3,core,sub_boundary=command-ingress; residual_owner=app:agent-notes`. Matches the pattern used by `unsigned-operation-rejection` (which also lives on `app:agent-notes`). |
| **B12** `cairn-18` ownership transfer | **CLOSED** | `MATRIX.csv:122` now `cairn-18,...,app:cairn,...,Phase 2,core,sub_boundary=portal-rendering; residual_owner=app:cairn; ASSUMPTION: a future dossier consolidation may transfer the surface, but 0A does not transfer this live defect`. `MECH-TAX:102` says the same. |
| **B13** five adversary rows demanded PASS on non-resist | **CLOSED** | §5 of `BAKEOFF-RUBRIC.md` now has explicit `Must-PASS (registry resists)` and `Expected residual (registry does not resist)` columns (`:78–87`). `ADV-03` (`:80`) moved `INV-005`/`INV-014` to expected-residual. `ADV-14` (`:83`) moved them to expected-residual and added an honest acknowledgement that `INV-051` is "detection, not prevention." `ADV-23` (`:84`) moved `INV-015` to expected-residual. `ADV-26` (`:85`) moved `INV-005`/`INV-014` to expected-residual. `ADV-27` (`:86`) moved `INV-015`/`INV-038` to expected-residual. §7.1 (`:107`) explicitly says *"A demonstrated non-resistance already conceded in its **expected residual** set is recorded as a residual and never disqualifies by itself."* |
| **B14** no invariant above INV-049, no CT family | **CLOSED (all four; break-glass is mixed)** | New workload rows added: `Concurrency / TOCTOU` (`RUBRIC:69`) cites `INV-026, INV-027, INV-054; CT`. `Unanchored outage and recovery` (`:70`) cites `INV-018, INV-055, INV-058`. `Break-glass` (`:71`) cites `INV-038, INV-049, INV-055`. Append row (`:60`) now cites `INV-051`. Common-controls (`:49`) cites `INV-052` for pin acquisition and first-contact. §3 (`:37`) now defines the **mandatory security core** as: *Append; Rotation; Revocation with effect range; Inclusion and consistency proof; Checkpoint and witness; Fork; Rollback; Restore security predicate; Projection rebuild; Concurrency/TOCTOU; Unanchored outage/recovery; Break-glass; and every section 5 adversary scenario.* That core's incompleteness is disqualifying; non-core rows can be `INCOMPLETE` without disqualification. See NEW BLOCKING on `INV-056` and `INV-059` citations. |
| **B15** Rotation predicate contradicts INV-011 residual | **CLOSED** | Rotation row (`:61`) now: *"PASS only if authority is evaluated at the authenticated prior position … Acceptance from a cut whose witnessed observation age exceeds the applicable action floor is FAIL."* §3 OPEN (`:52`) records the need to size the `PERMISSION_NOW_MAX_STALENESS` floor before freeze. The contradiction with INV-011's `action floor` is gone. |
| **B16** security re-weighted, double-counted | **PARTIAL** | §7.1 (`:112`) now adds INV-055 and INV-057 to the disqualifying invariant gate, closing the "5-point deduction" path. The independence gate is now phrased as *"Operational sustainability after the independence gate"* (`:123`) with anchors `0/1/3/5` and explicit text *"Independence itself earns no points."* However: (a) the criterion still has anchors `0` and `1` whose descriptions still describe a candidate that would already be DISQUALIFIED by §7.1's INV-016 line (`:109`). The wording *"post-gate operation is not repeatable"* presupposes the gate was passed. So the usable scoring range is effectively 3–5, which compresses the resolution but does not eliminate the conceptual defect — a candidate cannot meaningfully score 0 or 1 here because such candidates never reach §7.2. (b) The criterion still mixes "independence topology" (which is a gate) with "staffing/monitoring/runbooks" (which is operational); Opus's "double-counting" critique stands in spirit even though the word "independence" is no longer credited. **PARTIAL closure**: structurally improved; scoring-anchor language still ambiguous for the 0/1 case. |
| **B17** 20 points from break-glass not exercised | **PARTIAL** | `Break-glass` workload row added (`:71`); workload now exercises `INV-049` and `INV-055`. `ADV-34` row added to §5 (`:87`). §7.2 Recovery criterion (`:124`) now reads *"dual-control emergency flow and audit closure"* with anchors `0 = unsafe/incomplete recovery; 1 = manual recovery cannot demonstrate consistency`. So the 20 points are now scored against an actually-run row. **PARTIAL** because (a) the §7.2 anchors `0` and `1` still describe conditions that §7.1's break-glass gate would already disqualify, so the criterion's effective range is again 3–5; (b) the "Recovery, degraded state, and break-glass operability" criterion (`:124`) is **not** in the environment-fit list at `:134` — only Operational sustainability, Platform integration, Licensing, and Recovery are. That's actually consistent — break-glass is a security item, scored post-gate — but it means B17's "20 points for description" complaint is now structurally answered by the workload row, while the scoring-anchor duplication defect (`:124` lower anchors duplicate §7.1) remains. |
| **B18** time-box + freeze → universal INCOMPLETE | **CLOSED** | §3 (`:37`) defines the mandatory security core; (`:39`) requires owner dry-run sizing before freeze; (`:14`) `INCOMPLETE` is *"disqualifying"* only for the security core; non-core rows can be `INCOMPLETE` without disqualification (no favorable score). `OPEN:` to size the box before the freeze gate. This is the right structural answer. |
| **NB-1** 78 probe count | **CLOSED** | Taxonomy decision recorded at `MECH-TAX:14` (`OPEN:` accept ~78 or merge near-synonymous singletons); owner decision D-0A-7 accepts 78 probes. Probe count confirmed. |
| **NB-2** taxonomy inherits C-class assignments | **CLOSED (documented)** | `MECH-TAX:5` now explicitly states: *"102 of 103 mechanisms remain wholly within one C-class, so inherited C-class assignments were not independently re-adjudicated."* Disclosed in the status line. |
| **NB-3** `checked-evidence-required-for-pass` vs `aggregate-verdict-completeness` overlap | **NOT CLOSED (still distinct in v0.2)** | The two mechanisms remain separate in the taxonomy (`MECH-TAX:32` and `:80`). `cairn-16` → `checked-evidence-required-for-pass`, `cairn-02` → `aggregate-verdict-completeness`. The distinguishing predicate is not stated. Both feed `INV-022` (`TM:197`). Reading the definitions: `checked-evidence-required-for-pass` is about *unchecked evidence contributing to PASS* (one boolean aggregate); `aggregate-verdict-completeness` is about *PASS omitting unverified counts, halted replay, warnings, or inconsistent totals* (multi-field aggregation). The distinction is real but narrow and not stated. Reproduction set is unaffected. **Status: documented as a non-blocking gap; my read is the same as Opus's.** |
| **NB-4** `as-10` placed by definition widening | **NOT CLOSED (still widened)** | `MECH-TAX:36` still defines `subprocess-result-authenticity` as *"Parsed child **or remote** output is accepted despite process/transport failure"*. `as-10` is Medium and not in the 78-probe set, so impact is low. The widening stands. |
| **NB-5** second-component tie-break under-specified | **CLOSED** | `MECH-TAX:12` now: *"`ASSUMPTION:` 'a second component' means a different logical product where possible. The four Regista reports are one logical product for diversity … For mechanisms spanning three or four components, choose the second logical product by highest effective severity, then verified status, then the component order above; choose its finding by the same finding tie-break. This makes the `authenticated-approval-and-sod` choice of `an-1` derivable."* Derivable from a stated rule. |
| **NB-6** SEC severity scale ambiguity | **CLOSED** | `MECH-TAX:14` pins the six↔four-column mapping. `MECH-TAX:9` establishes the effective-severity ordering with SEC-01 at High (not Critical) and the example SEC-04/SEC-07 severities. The mapping is mechanical and pinned. |
| **NB-7** candidate-B independence scope too narrow | **CLOSED** | `RUBRIC:146` now names `directory/IdP administrator (`ADV-30`)` and `secret-backend administrator (`ADV-31`)` as part of the witness-independence perimeter. All five operator/exclusion classes from TM and v0.2 review are listed. |
| **NB-8** §8 blocks "environment-fit" without naming the criteria | **CLOSED** | `RUBRIC:134` explicitly enumerates the four environment-fit criteria: *"**Operational sustainability after the independence gate**, **Recovery/degraded-state/break-glass operability**, **Platform integration and driver ergonomics**, and **Licensing/supportability/lifecycle cost**."* Named, with `UNSCORED` treatment when the corresponding premise is `OPEN`. |
| **NB-9** premise checklist B-only | **CLOSED (asymmetric but explicit)** | `RUBRIC:173` adds: *"A symmetric premise/evidence ledger for A, B, and C. Each asserted dependency (including A's independent witness operator and C's licensing/support/staffing) is `EVIDENCED`, `FAILED`, or `OPEN`; `OPEN` supplies no favorable score. B additionally completes its chartered formal checklist."* The asymmetry is acknowledged and the rule applied symmetrically in scoring. |
| **NB-10** candidate A vocabulary no credit | **CLOSED** | `RUBRIC:29` explicit: *"Candidate A's use of the requirements vocabulary (cuts, signed log, checkpoints, witnesses, and proofs) confers no credit. A must produce the same measured evidence and pass the same predicates as B and C; matching vocabulary or asserting the target design is not measurement."* |
| **NB-11** B ledger verification inside SQL Server | **CLOSED** | `RUBRIC:31` explicit: *"For candidate B, verification executed inside SQL Server satisfies independence only when an external verifier can reproduce the digest-chain result from independently retained digests without trusting the live instance's verdict."* Named evidence: external verifier command/version, independent digest source and custody, input/output digests, tampered-instance negative test. Absence fails the local-verification predicate rather than pre-judging the product. |
| **NB-12** process/traceability | **PARTIAL** | (a) Pre-selection digest `sha256:5918…0651` is recorded in `REPRODUCTIONS.md:5` and `MECH-TAX:146`, providing a frozen evidentiary anchor for the membership and selection. (b) `MATRIX-SCHEMA.md:35` already marks initial rows as provisional, and rule 13 (`:46`) requires every row to be derivable from its taxonomy mechanism entry. However: every row's `notes` column still does **not** carry an explicit `PROVISIONAL:` prefix on the boundary/phase/disposition fields. I see `PROVISIONAL:` in 100% of the rows I sampled — actually let me re-verify: looking at the matrix output, every row's `notes` begins with `sub_boundary=...` followed by `; PROVISIONAL: ...`. So (b) is **CLOSED**. (c) `MECH-TAX:3` has version `v0.2-draft, 2026-08-26` and `REPRODUCTIONS.md:3` has version `v0.1-draft, 2026-08-26`. Both carry version headers. **CLOSED.** |
**Closure scorecard:** 18 of 18 B-items resolved (B16 and B17 PARTIAL but materially addressed); 12 of 12 NB-items resolved (NB-3 and NB-4 carried forward as documented non-blocking, NB-12 PARTIAL but resolved on second read). The drafter's response to Opus is materially complete.
---
## Attack on the new material
### 1. `REPRODUCTIONS.md` — 12-entry executor-readability audit
Picked one row per component plus a sample of trustlog/persist/cairn/agent-suite coverage. The audit question: could an executor run this without asking questions?
| Row | Verdict | Why it works / fails |
|---|---|---|
| `persist-5` (`:27`) | **RUNNABLE** | Fixture: *"Two connections and barriers around lock/commit."* Steps enumerate state (cache, cancel, commit). Confirmed observable: *"Cancelled operation commits."* Invalidated: *"Commit rereads locked state and denies/retries."* Severity-carrying observation: *"Critical authority transition after cancellation."* An executor knows what to build. The "barriers" wording is colloquial; could be sharper ("two open psycopg connections, both at READ COMMITTED, with explicit BEGIN ... SELECT ... barrier") but workable. |
| `cairn-01` (`:28`) | **RUNNABLE** | Fixture: *"Valid chain and filtered bundle."* Steps are three exact operations. Confirmed: *"Global gap is suppressed and result PASSes."* Invalidated: *"Global gap remains and result denies."* Severity: *"Critical false completeness verdict."* Concrete. |
| `cairn-02` (`:29`) | **RUNNABLE** | *"Signature-valid referent-missing events."* The fixture description is slightly dense — "referent-missing" is jargon for "event references a digest that is not in the bundle." An executor who hasn't read the inventory will need to look it up. Steps and observables are clear. |
| `acb-1` (`:30`) | **RUNNABLE** | Fixture: *"Attacker manifest, executable, and test secret reference."* Three steps with concrete outputs. Confirmed: *"Attacker executable receives selected secret/reference."* Invalidated: *"Manifest authentication/ownership denies before release."* Severity: *"Critical secret release to attacker code."* Concrete. |
| `SEC-08` (`:31`) | **RUNNABLE BUT THIN** | Fixture: *"Canonical enrollment and conflicting payload kind."* The fixture assumes the executor knows what "canonical enrollment" is and how to construct a "conflicting payload kind" — both are inventory concepts, not generic terms. An executor reading this cold will need to read the inventory entry for SEC-08. The observables are sound. Severity observation correctly notes the latent (no gate consumes it) — that's good honesty. |
| `persist-8` (`:32`) | **RUNNABLE** | Fixture: *"Unsigned workflow registration."* Three steps. Clear observables. Note the row does not specify *which* signed payload triggers "competing admission"; an executor must infer from the inventory. Acceptable for an executor with inventory access. |
| `cairn-15` (`:75`) | **RUNNABLE** | Fixture: *"Actor-only transition and operator/unknown role."* Steps and observables are crisp. Concrete. |
| `dossier-3` (`:76`) | **RUNNABLE BUT THIN** | Fixture: *"Replay output with halted count/warnings."* "Replay output" requires the executor to know what cairn's replay output looks like (the JSON schema). Steps 1-3 are fine. Confirmed: *"UI labels chain intact."* Invalidated: *"Halt/warning prevents intact claim."* Severity: *"False integrity badge."* The fixture leaves the schema unspecified; an executor will need a fixture-data example. |
| `as-13` (`:79`) | **RUNNABLE BUT THIN** | Fixture: *"Genesis verdict and first-write endpoint."* Steps: 1. Produce verdict. 2. Write without presenting/binding it. 3. Inspect state. Confirmed: *"First write succeeds."* Invalidated: *"Bound one-use verdict is required/consumed."* Severity: *"Bootstrap-first-write bypass."* The phrase "Produce verdict" assumes the executor knows how to trigger genesis — not obvious from the fixture column alone. An executor will need to consult the agent-suite bootstrap code to find the genesis-verdict endpoint. |
| `an-5` (`:65`) | **RUNNABLE** | Fixture: *"Resolvable authenticated actor plus null/spoof raw input."* Three steps. Confirmed: *"Event stores raw null/spoof instead of resolved actor."* Invalidated: *"Event stores canonical actor or denies mismatch."* The fixture assumes the executor knows how to feed a "null/spoof raw input" into agent-notes' command ingress while keeping the resolved actor authenticated. This is exactly the right probe shape, and the wording is just enough. Severity-carrying observation correctly notes "app ingress owns fix with kernel claim input" — that line tracks the residual_owner=app:agent-notes decision and would prevent the executor from over-reach. |
| `aw-7` (`:44`) | **RUNNABLE BUT THIN** | Fixture: *"Writable pending store and receiver."* "Writable pending store" — the executor needs to know the file path or table name for agent-wake's pending row store. The inventory presumably names it. Steps are concrete (insert forged row, drain queue, capture delivery). Confirmed/invalidated are clear. |
| `crypto-4` (`:93`) | **RUNNABLE** | Fixture: *"Windows DPAPI machine-scope fixture and public-identity flow."* The executor must set up a Windows machine-scope DPAPI context with known keys and then run the flow. Steps are concrete: 1. Invoke flow. 2. Capture argv/output blob. 3. As distinct allowed machine principal, attempt DPAPI decrypt and key use. The confirmed observable is split into *"Exposure: blob appears"* vs *"severity-confirming: adversary decrypts and uses private key."* This separation is exactly what B5 demanded. The severity observation explicitly notes *"High is carried by successful machine-scope adversary decrypt/use; argv/output presence alone confirms exposure at a lower/undetermined severity."* Excellent. The only minor weakness is "public-identity flow" — an executor must know which flow (likely regista's enrollment path); this should be cross-referenced to the inventory entry for crypto-4. |
**Overall on `REPRODUCTIONS.md`:**
- **Discriminator (`invalidated` vs `not-reproducible`)** is clearly stated at `:12–13`. Every row states a distinct invalidated observable. **Distinct.**
- **`confirmed` observable** is concrete on every row I sampled. **Real oracle.**
- **Severity-carrying observation** correctly separates exposure from severity in `crypto-4` and `as-5`. **Adequately disambiguated.**
- **105 lines for 78 entries** is dense. Average ~1.3 lines per entry (table cell). Some rows are 2 lines because of severity clarification; most are 1. **Dense but sufficient**, with one caveat: the entries are runnable by an executor who has access to the inventory and the component code; a cold reader would need to cross-reference. That's a documentation/taxonomy question, not a probe-spec question. Acceptable for the executor hand-off.
### 2. `MATRIX.csv` — deferred-profile elimination + spot-check of 15 rows
**Deferred-profile elimination: COMPLETE.** Grep confirms 0 occurrences of `deferred-profile` in `MATRIX.csv`. The 16 acb rows + 14 agent-wake rows that previously all read `deferred-profile, Phase 5` are now a mix of `partially-mitigated` (with `residual_owner=broker` / `residual_owner=transport` in notes) and `independently-patched`. Sample:
- `acb-1` (`:27`): `partially-mitigated, broker, residual_owner=broker, "no profile deferral"` — Critical is not deferred.
- `acb-5` (`:28`): `partially-mitigated, broker, residual_owner=broker` — High not deferred.
- `acb-9` (`:29`): `partially-mitigated, bootstrap-root, residual_owner=bootstrap-root` — High not deferred; ownership moved to bootstrap-root matching INV-035 home.
- `acb-10` (`:143`): `partially-mitigated, broker, residual_owner=broker, "current developer-host register path is reachable and not profile-deferrable"` — explicitly honest.
- `aw-7` (`:26`): `partially-mitigated, transport, residual_owner=transport` — High not deferred.
- `aw-11` (`:141`): explicitly *"not profile-deferrable"* in notes.
**Partial-mitigation with residual owners is honest.** A grader can read the row, see the owner, and know who is accountable. No row's owner contradicts its mechanism entry.
**Spot-check of 15 rows for owner-mechanism-TRUST-MODEL-home consistency:**
| Row | Owner | Mechanism entry says | TM invariant home | Match? |
|---|---|---|---|---|
| `SEC-05` (`:61`) | `bootstrap-root` | `bootstrap-root + kernel` | INV-009 (`bootstrap + kernel`) | ✓ (`residual_owner=kernel` recorded) |
| `as-13` (`:83`) | `bootstrap-root` | same | INV-009 | ✓ |
| `persist-13` (`:70`) | `bootstrap-root` | same | INV-009 | ✓ |
| `acb-9` (`:29`) | `bootstrap-root` | `bootstrap-root + broker` | INV-035 (`bootstrap`) | ✓ |
| `dossier-14` (`:79`) | `bootstrap-root` | `bootstrap-root` | INV-036 (`bootstrap`) | ✓ |
| `cairn-15` (`:75`) | `gate-engine` | `gate-engine` | INV-024 (`gate`) | ✓ |
| `SEC-10` (`:62`) | `gate-engine` | `gate-engine` | INV-028 (`gate`) | ✓ |
| `persist-7` (`:69`) | `gate-engine` | `gate-engine` | INV-028 | ✓ |
| `as-12` (`:82`) | `gate-engine` | `gate-engine` | INV-028 | ✓ |
| `an-1` (`:86`) | `gate-engine` | `gate-engine` | INV-028 | ✓ |
| `cairn-18` (`:122`) | `app:cairn` | `app:cairn for the current portal` | n/a (live defect, assumption) | ✓ (ASSUMPTION noted) |
| `an-5` (`:52`) | `app:agent-notes` | `app:agent-notes at command ingress, consuming a kernel identity/authority claim` | INV-053 (`bootstrap + each key owner`) | ✓ (`residual_owner=app:agent-notes`) |
| `cli-3` (`:65`) | `app:regista-cli` | `app:regista-cli` | TM:253 explicit 0A-3 per-component control | ✓ (`residual_owner=app:regista-cli`) |
| `acb-1` (`:27`) | `broker` | `bootstrap-root for suite artifacts and broker for capability manifests` | INV-035 + INV-030 | ✓ (`residual_owner=broker`) |
| `as-16` (`:85`) | `bootstrap-root` | `bootstrap-root for suite CLI findings` | n/a (suite CLI defect) | ✓ (`residual_owner=bootstrap-root`) |
**All 15 rows reconcile.** No owner contradiction found. The sub_boundary slug in notes matches the taxonomy's mechanism-level sub-boundary language.
### 3. `BAKEOFF-RUBRIC.md` — B13, B14, B16, B17, B18 verification
- **B13 closure (§5 adversary rows):** Re-derived against TM:
  - `ADV-03` (`:80`): must-PASS = `INV-004, INV-006, INV-007, INV-016–18, INV-021, INV-022, INV-051`. Residual = `INV-005, INV-014`. Per TM:176 INV-001 does-not-resist ADV-03? No, INV-001 *resists* ADV-03. INV-005 (TM:180) does-not-resist ADV-03 ✓. INV-014 (TM:189) does-not-resist ADV-03 ✓. The must-PASS set is consistent.
  - `ADV-14` (`:83`): must-PASS = `INV-010, INV-016–18, INV-021`. Residual = `INV-005, INV-014`; `INV-051` annotated as "detection, not prevention." TM:185 (INV-010) resists ADV-14 ✓. TM:189/192/193 (INV-014/017/018) does-not-resist ADV-14 — **WAIT**: TM:185 INV-010 has does-not-resist ADV-14 (yes — see TM:185 "ADV-14" in does-not-resist column). But the rubric puts INV-010 in must-PASS. **This is a regression — see NEW BLOCKING B-NEW-1.**
  - `ADV-23` (`:84`): must-PASS = `INV-016–18, INV-021, INV-022, INV-049`. Residual = `INV-015`. TM:190 (INV-015) does-not-resist ADV-23 ✓. TM:191–193 (INV-016–18) all resist ADV-23 ✓. Consistent.
  - `ADV-26` (`:85`): must-PASS = `INV-004, INV-006, INV-016–18, INV-021, INV-038, INV-051`. Residual = `INV-005, INV-014`. TM:179 (INV-004) does-not-resist ADV-26 ✓. TM:180 (INV-005) does-not-resist ADV-26 ✓. TM:181 (INV-006) does-not-resist ADV-26 ✓. Consistent — except TM:213 (INV-038) **does-not-resist** ADV-26 (in its resistance list, ADV-26 is NOT in INV-038's "Resists" column at TM:213 — let me re-check). TM:213 lists INV-038's Resists as ADV-01,02,03,04,05,06,07,09,10,11,12,13,15,17,18,20,21,22,23,24,26,28,29,30,31,34 — wait, **ADV-26 IS in INV-038's Resists** (TM:213). So INV-038 resists ADV-26. The rubric's must-PASS `INV-038` for ADV-26 is consistent.
  - `ADV-27` (`:86`): must-PASS = `INV-014, INV-016–18, INV-022`. Residual = `INV-015, INV-038`. TM:190 (INV-015) does-not-resist ADV-27 ✓. TM:213 (INV-038) does-not-resist ADV-27 ✓. Consistent.
  - `ADV-34` (`:87`): must-PASS = `INV-055`. Residual = `INV-049`. TM:224 (INV-049) does-not-resist ADV-34 ✓. TM:230 (INV-055) — let me check. TM:230 lists INV-055's Resists as ADV-01..07, 09..18, 20..24, 26..31, 34 — **ADV-34 is in INV-055's Resists** ✓. Consistent.
  **One regression found: ADV-14's `INV-010` citation is wrong.** See NEW BLOCKING below.
- **B14 closure (new workload rows):**
  - Append (`:60`): cites `INV-001–005, INV-022, INV-051`. Now includes `INV-051` (signed ack + deadline + independent channel). ✓
  - Concurrency / TOCTOU (`:69`): cites `INV-026, INV-027, INV-054; CT`. New family CT now exercised. ✓
  - Unanchored outage and recovery (`:70`): cites `INV-018, INV-055, INV-058`. Now exercises INV-058. ✓
  - Break-glass (`:71`): cites `INV-038, INV-049, INV-055`. INV-049 cumulative cap now exercised. ✓
  - Common controls (`:49`): cites `INV-052` for pin acquisition. ✓
  - §3 (`:37`) defines mandatory security core vs INCOMPLETE-tolerant split. ✓
  **One coverage gap remains: `INV-056` (unselected-policy denial) and `INV-059` (exact-subject human/agent review admission)** are explicitly out-of-scope per `RUBRIC:136`. §5 adversary rows do not exercise them. The plan's review cells cite INV-056 (TM:231) and INV-059 (TM:234) as load-bearing for claim issuance; the rubric's framing is "they receive no implied PASS from substrate results," which is honest but means an executor reading the rubric will not test claim-issuance refusal under unselected policy or exact-subject admission. Whether that is a deliberate scoping decision or a coverage gap depends on the 0B charter. **Not a defect for the rubric per se; flagged for owner awareness.**
- **B16 closure (security re-weighted):**
  - INV-055 and INV-057 now in §7.1 disqualifying gate (`:112`): ✓
  - Operational sustainability criterion renamed *"after the independence gate"* with anchors 0/1/3/5 and explicit *"Independence itself earns no points."* ✓
  - But anchors `0` and `1` for this criterion describe conditions that are already §7.1-gate-disqualifying (failed INV-016). So the criterion's usable scoring range is 3–5, and the `0` and `1` anchors are vestigial. The §7.2 anchor table still reads as if candidates scoring 0 or 1 on independence are reachable, contradicting §7.1. **Logical incoherence at the criterion level.** This is the "double-counting" defect re-named but not eliminated — see NON-BLOCKING NB-NEW-1.
- **B17 closure (20-point break-glass evidence):**
  - Break-glass workload row now present (`:71`), exercising `INV-049`'s cumulative cap and chained pre-numbered records.
  - `ADV-34` row in §5 (`:87`) exercises emergency-approver collusion against `INV-055` floor enforcement.
  - §7.2 Recovery criterion (`:124`) anchors 0–5: scoring requires `dual-control emergency flow and audit closure`, which is now evidence-based via the workload row.
  - However: the Recovery criterion is not in the environment-fit list at `:134`. So an `OPEN` Recovery premise does **not** make it `UNSCORED` — it would still receive a weighted score. That's actually correct (Recovery is a security post-gate criterion, not an environment-fit criterion). But the anchor language `0 = unsafe/incomplete recovery; 1 = manual recovery cannot demonstrate consistency` describes conditions that the §7.1 break-glass disqualifier (`:111`) already catches. Same anchor-duplication defect as B16. **PARTIAL — structural improvement, but criterion-level scoring-anchor redundancy with §7.1 remains.**
- **B18 closure (time-box + universal INCOMPLETE):**
  - §3 (`:37`) security core defined and `INCOMPLETE` is disqualifying only for the core.
  - §3 (`:39`) requires owner to size the 5-day box against a dry run before freeze.
  - §3 `OPEN:` preserves the option to revise and re-freeze the rubric if the dry run shows the box is too small.
  - **CLOSED.**
---
## VERDICT per work product
| Work product | Verdict |
|---|---|
| **0A-2** — mechanism taxonomy + reproduction set | **accept-with-changes** |
| **0A-3** — matrix schema + 147-row matrix | **accept-with-changes** |
| **0A-5** — bake-off rubric | **accept-with-changes** (one regression to fix; B16/B17 scoring-anchor defect remains) |
---
## NEW BLOCKING
**B-NEW-1 — `BAKEOFF-RUBRIC.md:83` (ADV-14 row): `INV-010` is cited in must-PASS, but `TRUST-MODEL.md:185` explicitly places ADV-14 in INV-010's `Does not resist` set.**
This is a regression: the entire reason the rubric was reworked was to split must-PASS from expected-residual (B13). On the ADV-14 row, INV-010 is now in must-PASS, but TM says INV-010 does not resist ADV-14. An executor who follows the rubric would conclude that *writer host root* must be defeated by *authority-position/expiry* — which is the precise non-resistance the trust model documents (TM:185: *"INV-010 | … | ADV-03, ADV-08, ADV-14, ADV-15, ADV-19, ADV-22, ADV-25, ADV-28, ADV-32, ADV-33 | …"*). The rubric's `Expected detection/refusal behavior` for ADV-14 (`:83`) already concedes this — *"Independent history below quorum remains detectable; do not claim writer forgery prevention"* — but the must-PASS column does not match.
*Fix:* move `INV-010` to the expected-residual column for ADV-14. Add `INV-013` (which resists ADV-14 per TM:188) and `INV-051` (which is also currently in the residual annotation — promote it to must-PASS, since TM:226 has ADV-14 in INV-051's does-not-resist list — wait, INV-051 does NOT resist ADV-14 either per TM:226. So INV-051 stays in residual.) The likely correct must-PASS for ADV-14 is just `INV-016–18, INV-021` (all in INV-016's family that ADV-14 cannot reach because witnesses are independent of writer host). Add `INV-013` (one-step transitive propagation that the writer host can poison) — TM:188 lists ADV-14 in INV-013's does-not-resist. So actually the cleanest answer is `must-PASS = INV-016–18, INV-021` with `residual = INV-005, INV-010, INV-013, INV-014, INV-051`. That matches what the rubric already says in prose (`Independent history below quorum remains detectable`).
---
## NEW NON-BLOCKING
**NB-NEW-1 — `BAKEOFF-RUBRIC.md` §7.2 criterion anchors still describe pre-gate states (B16/B17 carry-over).**
Operational sustainability (`:123`) anchors `0 = post-gate operation is not repeatable; 1 = heroic/manual and unowned despite compliant topology` — but a candidate scoring `0` or `1` here would already be §7.1-disqualified by INV-016 (`RUBRIC:109`). Same for Recovery (`:124`) anchors `0 = unsafe/incomplete recovery; 1 = manual recovery cannot demonstrate consistency` — those are §7.1-disqualifying by `INV-038`/`INV-049` (`:111`). The criterion ranges are effectively 3–5 even though the table reads 0–5. Either (a) rewrite the anchors so 0/1 are post-gate-reachable (e.g. `0 = post-gate operation is repeatable only with material unstaffed gap; 1 = repeatable but with periodic ownership churn`), or (b) add a header note: *"Effective scoring range for any candidate that passed §7.1 is 3–5; anchors 0 and 1 are retained for completeness against an ineligible-candidate input and do not produce a score."*
**NB-NEW-2 — `MECHANISM-TAXONOMY.md` and `MATRIX-SCHEMA.md` are versioned but their schemas are not.** The mechanism-taxonomy has `Version: v0.2-draft, 2026-08-26` (`MECH-TAX:3`); the schema carries `Status: DRAFT` but no version/date (`MATRIX-SCHEMA.md:3`). Adding `Version: v0.2-draft, 2026-08-26` (matching the taxonomy) and `Last reviewed: third-lineage reviewer 3, 2026-08-26` would mirror the rubric/trust-model convention and make the freeze gate reference a version rather than a path. Not load-bearing but consistent with the rest of the plan's hygiene.
**NB-NEW-3 — `REPRODUCTIONS.md` does not specify a fixture-data contract.** Rows like `dossier-3` (`:76`, "Replay output with halted count/warnings"), `as-13` (`:79`, "Produce verdict"), and `an-9` (`:66`, "Signed outbox item") name concepts that require the executor to know the JSON schema and command surface. A small appendix *"Fixture data shapes"* pointing to the inventory's component schemas (or stating "use the most recent reviewed-ref default test fixtures") would close the gap. Acceptable for an executor hand-off, but the rubric's §3 time box (`:35`) will eat time on every row that has to derive the fixture shape.
**NB-NEW-4 — `MATRIX-SCHEMA.md:46` rule 13 requires `sub_boundary` to be derivable from the taxonomy mechanism entry.** Spot-checked 5 rows: each row's `sub_boundary=` slug matches a clause in its mechanism entry. But the schema does not say *how* a grader verifies derivability (machine-checked? line-citation?). Add: *"Grader may verify by reading the mechanism entry and confirming the `sub_boundary` slug appears verbatim in the entry's `Owning boundary:` sentence or in the same mechanism's preceding close-it clause."*
**NB-NEW-5 — `BAKEOFF-RUBRIC.md:37` mandatory security core includes "every section 5 adversary scenario" but §5 has 7 scenarios and `ADV-12`/`ADV-13` are also stress-load-carrying in §4.** Worth stating explicitly that the §4 workload rows *exercising* the §5 adversary injection (e.g. Fork row tests ADV-26/ADV-27) cover the §5 cross-reference without duplication, so the executor runs each §5 injection once against each candidate rather than once per workload row. Not a defect; clarifying.
**NB-NEW-6 — `MECHANISM-TAXONOMY.md:14` carries an `OPEN:` that overlaps with `MATRIX-SCHEMA.md:64` OPENs on the `CTRL-nnn` namespace.** Both files have open owner questions. Worth consolidating into one owner-decision list (the existing `DECISIONS-0A.md` would be the natural home).
**NB-NEW-7 — The `REPRODUCTIONS.md` pre-selection digest `sha256:5918f63a…0651` is the digest of the taxonomy at the v0.1 state, not v0.2.** `MECH-TAX:146` acknowledges this: *"this v0.2 revision changes ownership and moves `persist-9` within the INV-005 family but does not alter the verified 78-finding selection."* That is correct — the 78-finding selection is identical between v0.1 and v0.2. But the digest itself would only match v0.1. Worth adding a one-line note that the digest is of the pre-selection state and that v0.2's owner/INV-005 re-derivation is non-membership-altering, so the same 78 IDs apply. Already there at `MECH-TAX:146` — surface-level fine, but a future auditor checking the digest against v0.2 will see a mismatch unless this note is right at the digest citation. Minor.
---
## Charter acceptance criteria
### 0A-2 (`README.md:29-37`)
| Criterion | Status |
|---|---|
| Mechanism taxonomy: each of the 147 findings tagged with a mechanism finer than C1–C5 | **met** — independently verified: 147 unique ids across 103 mechanisms |
| Taxonomy written before selection and committed | **met** — pre-selection digest `sha256:5918…0651` recorded at `MECH-TAX:146` and `REPRODUCTIONS.md:5` |
| Selection rule correctly applied for representative Highs | **met** — 78 rows = 59 High-bearing + 18 second-component + 1 mandatory Critical extra |
| All 7 Criticals reproduced unconditionally | **planned, not attempted** — all 7 present (`persist-1`, `persist-2`, `persist-4`, `persist-5`, `cairn-01`, `cairn-02`, `acb-1`); no probe executed |
| Every High-bearing mechanism has ≥1 attempted reproduction | **planned, not attempted** — 59+ mechanisms covered in the plan; no probe executed |
| Each reproduction is an executable probe with observed outcome and verdict | **planned, not attempted** — 78 spec rows with confirmed/invalidated/severity observables; verdict blocks blank |
| Probes committed and re-runnable | **not met** — no probes exist; this is the executor's deliverable, not the drafter's |
| Verdicts reviewed by a second lineage | **not met** — nothing to review |
| Inventory severities updated with a `verified` column | **not met** |
| Deliverable at `plans/025-0a/REPRODUCTIONS.md` | **met** — file exists, 105 lines, 78 spec rows + verdict contract + executor-completion note |
The charter distinguishes *the artifact* from *the execution of the artifact*. The drafter has produced the artifact; the executor work (Opus probe executors + Daybreak Blue) is sequenced after the 0A exit. The file honestly states its status as "EXECUTOR HAND-OFF" and concludes with *"Until those actions occur, the charter criteria … remain unmet."* That is the correct posture.
### 0A-3 (`README.md:39-45`)
| Criterion | Status |
|---|---|
| 147 rows present | **met** — exactly 147 data rows; one per inventory id, no duplicates; component tallies match |
| Every row has `mechanism` | **met** — all nonempty, all declared taxonomy slugs |
| Every row has provisional `disposition` | **met** — all 147 non-empty; 0 `deferred-profile` (eliminated); mix of `partially-mitigated` and `independently-patched`; every row carries `sub_boundary=…` slug |
| Every row has `owning_boundary`, none empty | **met** — enum expanded to include `app:regista-cli` and `app:cairn`; all 147 rows populated; 15-row spot-check shows consistency with mechanism entry and TM invariant home |
| Every `fully-closed-by-kernel` row cites an `INV-` id | **met** — zero such rows; `MATRIX-SCHEMA.md:61` explains why. Honest. |
| Committed schema matches the charter's 16 columns | **met** — header is exact |
| Schema reviewed by a second lineage (this schema is itself gate infrastructure, WI-008 two-reviewer rule) | **met** — third-lineage reviewer 3 concurs on the schema (this review); two reviewer-3 reviews (REVIEW-0A1-minimax + this delta) confirm the schema is sufficient |
### 0A-5 (`README.md:55-59`)
| Criterion | Status |
|---|---|
| Rubric committed before 0B starts | **met** — committed at `HEAD` (4ab32a5); §1 OPEN records freeze gate |
| No candidate pre-scored | **met** — no scores anywhere; §1.7 explicit |
| Candidates A/B/C defined with neutral test boundaries | **met** — §2, with B independence evidence requirement per §2 paragraph 3 |
| Falsification workload: append, rotation, proof, checkpoint, fork/rollback, restore, representative reads | **met arithmetically** — all 7 + revocation-with-effect-range + projection rebuild; **met behaviorally** with new rows for Concurrency/TOCTOU (`INV-054`), Unanchored outage (`INV-058`), Break-glass (`INV-049, INV-055`) |
| Adversary scenarios from 0A-1 by invariant id | **met with one regression** — 7 scenarios present; columns split into must-PASS vs expected-residual; `INV-051, INV-052, INV-054, INV-055, INV-057, INV-058` are all now cited somewhere; **INV-010 mis-cited in `ADV-14` must-PASS column (B-NEW-1)** |
| Operational-requirements table columns (plan §6) | **met** — seven columns, all three candidates populated |
| Weights | **met arithmetically** — 7 criteria = 100; **PARTIAL closure on security-re-weighted complaint (B16/B17 anchor redundancy)** |
| Premise-validation checklist for B (5 chartered elements) | **met** — all 5 present at §8; ADV-30/ADV-31 added (NB-7 closed); B-specific eligibility failure mode stated at end of §8 |
| Language/stack criteria recorded for 0C | **met** — §9, five criteria, correctly marked decision-criteria-only |
| Two lineages concur | **PARTIAL** — reviewer 3 (this delta) concurs on B1–B15, B18, NB-1..NB-12; **does not concur** on B16/B17 anchor-duplication; **finds one regression** at ADV-14 INV-010 citation |
---
## One-line statement per work product
- **0A-2 (taxonomy + REPRODUCTIONS.md):** **READY for owner sign-off / freeze**, contingent on the reproduction executors (Opus + Daybreak Blue on mvmcc02) running the 78 probes, populating the verdict blocks, and updating the inventory `verified` column. The artifact itself is materially responsive to all Opus B-items and 11 of 12 NB-items; NB-3 and NB-4 are documented as carried-forward non-blocking.
- **0A-3 (MATRIX-SCHEMA.md + MATRIX.csv):** **READY for owner sign-off / freeze**. The 147 rows are populated, owner-discipline is honest, deferred-profile is eliminated, sub-boundary accountability is preserved at the notes level, and spot-checked owner-mechanism-TM-home consistency holds. No new blocking; six non-blocking hygiene items (NB-NEW-1..6) are documentation-only.
- **0A-5 (BAKEOFF-RUBRIC.md):** **NOT YET READY for freeze**. One regression to fix (B-NEW-1: ADV-14 row's `INV-010` must-PASS citation contradicts TM:185) and one partial-closure carry-over (NB-NEW-1: §7.2 anchors 0/1 describe pre-gate states for two criteria). Once B-NEW-1 is fixed and the anchor language is tightened, the rubric is ready for the two-lineage concur step and then owner freeze.
**End of review.**
