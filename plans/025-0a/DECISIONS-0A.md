# Phase 0A — owner decision list

Consolidated from the `OPEN:` markers in `TRUST-MODEL.md` v0.3 (accepted by three lineages:
Sol draft → Fable + Opus review → minimax third-lineage verdict *accept*), `DATA-BOUNDARY.md`,
`MECHANISM-TAXONOMY.md`, and `BAKEOFF-RUBRIC.md`. Each item carries the drafters' recommended
default so a one-word answer suffices. None blocks drafting; all block **0A exit**.

| # | Decision | Recommended default | Where it bites |
|---|---|---|---|
| D-0A-1 | **Free text in kernel events** — (a) forbid entirely, or (b) redactable-by-construction (salted per-field digest in chain, plaintext in a deletable side store). | **(a) forbid.** Ties directly to the PHI non-goal; (b) consciously narrows that non-goal and creates incident liability. → **DECIDED 2026-08-25: (a) forbid free text.** Plus a new requirement (D-0A-11): session histories must still be captured and made searchable/useful — outside the kernel. | `TRUST-MODEL.md` §11.5, INV-046/048 (inactive under (a)); `DATA-BOUNDARY.md` §1 "approved short text" class is then dropped. |
| D-0A-2 | **Legacy v1–v5 evidence** — reject / quarantine-as-unverifiable / one-time re-anchor. Must be decided before the first slice admits any legacy evidence. | **quarantine-as-unverifiable**, converting to rejection after a dated compatibility window. → **DECIDED 2026-08-25: quarantine-as-unverifiable.** | `TRUST-MODEL.md` §10, INV-050; migration phase entry gate. |
| D-0A-3 | **Retroactive revocation policy** — issuer roles, maximum lookback, dual-control roles, whether finite `effect_through` is allowed. | Deny issuance until set (current behaviour); propose: bootstrap-root operator + security approver as dual control; lookback unbounded; finite effect-through allowed. → **DECIDED 2026-08-25: recommendation accepted** (deny until set; bootstrap-root operator + security approver dual control; unbounded lookback; finite effect-through allowed). | §5, INV-013, INV-028. |
| D-0A-4 | **Staleness / cadence parameters** — `PERMISSION_NOW_MAX_STALENESS`, per-action floors, `CHECKPOINT_MAX_AGE`, `WITNESS_CLOCK_SKEW_MAX`, `UNANCHORED_WINDOW_MAX`, `WITNESS_RETENTION_MIN`, external time-reference set. | Defer to **0C** with 0B evidence; keep v0.3's proposed hard maxima (15 min / 60 min) as the ceiling the bake-off must meet. Propose `WITNESS_RETENTION_MIN` = 7 years (development artifacts, non-PHI) for 0B costing. → **DECIDED 2026-08-25: suggested figures accepted** as the hard maxima the bake-off must meet; final values in 0C. | §9, INV-011/012/015/055/058. |
| D-0A-5 | **Break-glass** — organisational roles for the two approvers; `BREAK_GLASS_INCIDENT_MAX` (proposed 60 min cumulative/incident) and `BREAK_GLASS_CROSS_INCIDENT_MAX` (proposed 120 min per rolling 24 h, same approver set). | Accept proposed caps for 0B costing; name roles at 0C. → **DECIDED 2026-08-25: proposed caps accepted**; roles named at 0C. | §9, INV-049/055. |
| D-0A-6 | **Data-boundary incident roles** — incident owner, notification path, deletion authority, retention/legal-hold policy. | Placeholders until the target environment exists; must be named before any production data. → **DECIDED 2026-08-25: accepted on the condition** that the placeholders stay placeholders until the environment exists and are named before any production data. | `DATA-BOUNDARY.md` §5, INV-048. |
| D-0A-7 | **Reproduction-set size** — accept ~78 probes (72 of the 80 ≥High findings, all 7 Criticals) or merge near-synonymous singleton mechanisms first. | **Accept 78.** Owner posture is "no rush, do it right"; 90 % of severe findings reproduced before freeze is the point. → **DECIDED 2026-08-25: accept 78** (70 executed by end of 08-25; see plan-025-evidence). | `MECHANISM-TAXONOMY.md` selection semantics; `REPRODUCTIONS.md`; 0A-2 executor budget (Opus + Daybreak on mvmcc02). |
| D-0A-8 | **Taxonomy tie-break and "High-bearing = effective severity ≥ High" definitions** (changing either recomputes the set). | Accept as written. → **DECIDED 2026-08-25: accept as written.** | `MECHANISM-TAXONOMY.md` §Selection semantics. |
| D-0A-9 | **Bake-off time box** — size the box against the mandatory security core before the rubric freeze (v0.3 rubric proposes 5 engineer-days/candidate, flagged as likely short by Opus B18). | Decide after Sol's round-2 rubric lands; expect to widen. → **DECIDED 2026-08-25: run the three candidates IN PARALLEL** — one executor per candidate against the shared frozen harness, one independent observer; the per-candidate engineer-day box stays, wall-clock collapses to ~1 week. Caveat: mvmcc03 (6 vCPU/16 GB) cannot host SQL Server 2022 + PostgreSQL + immudb + three executors comfortably — plan one candidate per host (mvmcc03, mvmcc02, mvmcitest01 for B on Windows) or sequence B separately. | `BAKEOFF-RUBRIC.md` §3, §1.6. |
| D-0A-10 | **Control-id namespace** — `CTRL-nnn` format for the matrix `control_id` column and the taxonomy `OPEN:` on counting/tie-break rules (overlapping OPENs in `MATRIX-SCHEMA.md` and `MECHANISM-TAXONOMY.md`). | Accept `CTRL-nnn`; counting rules as written (see D-0A-8). → **DECIDED 2026-08-25: recommendation accepted.** | `MATRIX-SCHEMA.md` §Columns; `MECHANISM-TAXONOMY.md` §Selection semantics. |
| D-0A-11 | **Session-history capture (NEW, from D-0A-1)** — capability B: capture Claude Code / opencode / codex session transcripts and make them searchable and useful, *without* putting transcript content in the kernel. Design shape: a **session-capture store** as a non-trust-bearing app in its own access-controlled substrate (per `DATA-BOUNDARY.md` §2 "content stays in the owning system"); the kernel holds only session digests, ids, and bounded descriptors (`INV-044`, `INV-047`); dossier retrieves content from the store under that store's access control; PHI scanning/retention policy applies to the store, not the chain. | 0A-4/0C design input; Phase-1 slice should include one captured session linked by digest so the demo shows "reviewable by a person." | New §; `ADMISSIBILITY-MAP.md` (chain-of-custody of the transcript = digest + store record); Phase 1 scope. |

Already decided (2026-08-25): D1 four-boundary decomposition; D2 Phase-0A go; D3 compliance
validation stage 1 only (informal, opportunistic), stage 2 deferred.

Sign-off record: D-0A-1..10 decided by owner 2026-08-25 (this conversation); `TRUST-MODEL.md` v0.3 OPEN items thereby resolved except deployment values deferred to 0C by D-0A-4/5/6. Formal TRUST-MODEL sign-off: owner: Paul Merritt  date: 2026-08-25 (per D-0A decisions; v0.3 text stands).
