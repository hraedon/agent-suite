# 0A-1 TRUST-MODEL.md v0.3 — reviewer 3 DELTA re-verdict (minimax-m3, third lineage, 2026-08-25)

# 0A-1 TRUST-MODEL.md v0.3 — reviewer 3 DELTA re-verdict (minimax-m3, 2026-08-25)
_Comparing v0.3 (`plans/025-0a/TRUST-MODEL.md`, L1–371) against reviewer 3's v0.2 findings (`REVIEW-0A1-minimax.md`). Text-only; no gate transition recorded. Cited line numbers are to v0.3._
## Closure table — the three blocking items
### NB-B1 — INV-055 monotonicity scope (now protects cache window + checkpoint cadence)
**VERDICT: CLOSED.** v0.3 §8 L230 expands INV-055 from "witness quorum/independence, retire-list coverage, algorithm/version floor, custody requirement, or unanchored-window hard maximum" (v0.2 L211) to enumerate explicitly: `PERMISSION_NOW_MAX_STALENESS`, per-action staleness maxima, revocation-publication bound, `CHECKPOINT_MAX_AGE`, `WITNESS_CLOCK_SKEW_MAX`, `UNANCHORED_WINDOW_MAX`, `BREAK_GLASS_INCIDENT_MAX`, `BREAK_GLASS_CROSS_INCIDENT_MAX`, plus a *raised* ceiling on `WITNESS_RETENTION_MIN`. The resistance column is then re-stated as unchanged. §9 L257 ("No runtime configuration or emergency policy may raise a hard maximum or lower a security minimum (`INV-055`)") is now backed by a normative invariant, not prose. The §9 `ASSUMPTION:` block at L257 also enumerates the same set, giving the closed loop. Minor caveat: §9 L257 names `WITNESS_RETENTION_MIN` only as a "minimum" while INV-055 L230 lists it among "security minimums" — directionality is consistent (no lowering), so the policy is uniform.
### NB-B2 — §7 "R" cell semantics (now uniform, with row-difference derivation)
**VERDICT: CLOSED.** v0.3 §7 L136 introduces a single normative cell semantic in prose: *"R means the claim structure, considered alone with all cited invariants and stated platform preconditions holding, resists the adversary by preventing a false positive; denial or loss of availability does not make the cell D."* It further clarifies that a registry-level Resists entry is "necessary evidence, not by itself sufficient" and that cells are evaluated *jointly* over the cited set. This is reading (a) from my v0.2 §B2 reconciliation — and is the only one consistent with the current matrix.
**Documented reading of "R":** *Claim-structure-alone, with cited invariants + platform preconditions holding → no false positive is producible; DoS/availability loss ≠ D.* Both `AttributableAuthorship`×ADV-09 (D, L142) and `CapabilityGrant`×ADV-09 (R, L144) follow cleanly: the former requires INV-059 (review admission), which does-not-resist ADV-09 (L234); the latter cites INV-030–033 (broker enforcement), which ADV-09 (agent-notes) cannot defeat. INV-053's key-custody resist of ADV-09 is necessary but not sufficient because the row-level owner of `AttributableAuthorship` (gate+app) is ADV-09-controllable; the broker owner of `CapabilityGrant` is not.
**Five-cell check (claim, adversary):**
1. (`IndependentReviewAttestation`, ADV-09) = **D** (L140). Cited: INV-041, INV-059. INV-059 (L234) does-not-resist ADV-09 (in the resistance column) → joint evaluation has a hole → D. **Consistent.**
2. (`CapabilityGrant`, ADV-09) = **R** (L144). Cited: INV-030–033, INV-053, INV-054. ADV-09 (agent-notes) cannot defeat broker enforcement; INV-053's non-exportability prevents agent-notes from holding broker keys. **Consistent.**
3. (`TamperEvidentChangeRecord`, ADV-03) = **R** (L141). Cited: INV-005 (chain), INV-014 (checkpoint), INV-016 (witness quorum), INV-051 (signed ack), INV-053 (key custody), INV-058 (recovery marker), INV-018 (fork). Joint evaluation: censorship + forgery defended by ack+quorum+HSM+marker. **Consistent.**
4. (`TrustedTimestamp`, ADV-30) = **R** (L143). Cited: INV-014, INV-016, INV-017, INV-052, INV-056 (prerequisite). Directory admin cannot forge TSA-witnessed timestamps because checkpoint binding runs through `INV-014`-`INV-018` independent of directory. **Consistent** (note: I had this as D in v0.2 §L160; v0.3 re-derived it to R and the cell-difference table L164 explicitly justifies it).
5. (`AttributableAuthorship`, ADV-14) = **D** (L142). Cited: INV-008 (writer-side acceptance), INV-053. INV-008's owner is the writer; ADV-14 controls writer host root. **Consistent** with L159.
The cell-difference derivation table L151–166 explains every adversary whose cells differ across rows by named invariant presence/absence, not by a different meaning of R. The uniform semantic is achieved.
### NB-B3 — INV-046/048 vacuity under no-free-text default
**VERDICT: CLOSED.** v0.3 §8 L221 (INV-046) and L223 (INV-048) now state the guards explicitly: *"If signed policy admits textual retention, … Under the no-free-text default, this conditional control is inactive rather than claimed as an exercised defense"* and analogously in INV-048. §11 element 2 (L283) and element 4 (L285) carry the same conditional framing, and §11 element 5 (L286) makes the dependency crisp: *"Until the owner selects (b), free text is forbidden and conditional INV-046/INV-048 retention branches are inactive."* The invariant statements are no longer vacuously true under default; they are explicitly **inactive** under default, which is the correct posture. Resist/do-not-resist columns remain populated (the controls *would* fire if retention were enabled), so the registry column structure is honest about what is conditional vs. what is currently live.
## Closure table — fifteen non-blocking items
| # | v0.2 finding | Status | Citation |
|---|---|---|---|
| NB-1 | `CapabilityGrant` should cite INV-051 | **CLOSED** | L144 invariant_ids now includes INV-051. |
| NB-2 | §5 Step 3 ↔ Step 5 interaction ambiguous | **CLOSED** | L77 explicitly resolves: exemption wins over effect-range marking; L79 mirrors the precedence ("Direct actions in an operative effect range remain marked by step 4 whether or not they confer authority"). My corner-case concern is resolved. |
| NB-3 | Sub-delegations after original grant not crisp | **CLOSED** | L77: "The exemption applies only to that recorded acceptance-time chain; it does not exempt later sub-delegations." L79: sub-delegation is marked by Step 5 when its derivation includes ineffective authority. |
| NB-4 | `AttributableAuthorship`×ADV-26 ADV-32 seam | **CLOSED** | L149: "‡ `AttributableAuthorship` x `ADV-26` is `R` only while the signer-key custody required by `INV-053` remains intact. Combining `ADV-26` with `ADV-32`, or using an HSM-unavailable profile … is outside that cell and is `D`." |
| NB-5 | INV-016 platform-testability gap | **CLOSED (correctly deferred)** | No normative change; Appendix A v0.2→v0.3 L361 records it as deployment-profile scope. Acceptable. |
| NB-6 | §11 (b) contradicts §1 non-goal | **CLOSED** | L286: "(b) … consciously narrows that non-goal by admitting textual ingestion and creates the associated compliance and incident-response liability; it is not merely an implementation variant of (a)." |
| NB-7 | INV-051 "independent channel" undefined | **CLOSED** | L226 INV-051 names the `INV-016` witness set as the default and requires an equivalent operator/key/admin/host/persistence-plane predicate for any policy-selected alternative. |
| NB-8 | INV-053 HSM-unavailable profile | **CLOSED** | L228: HSM-unavailable profile must declare affected key class, use strongest available keystore, mark INV-053 non-resistant to host-root + ADV-32, and re-derive dependent cells. |
| NB-9 | Break-glass across incidents unbounded | **PARTIAL — open by design** | §9 L268 proposes `BREAK_GLASS_CROSS_INCIDENT_MAX = 120 minutes` per rolling 24 hours for same approver set + affected boundary across all incident ids. Owner approval remains open. My v0.2 finding was that §9 lacks a cap; v0.3 supplies a normative proposed default and marks it owner-open, which is the right posture for a trust-model artifact. **CLOSED-AS-OPEN** (correctly delegated to owner). |
| NB-10 | Witness clock skew vs. external reference | **CLOSED** | §9 L257: signed external time-reference set + `WITNESS_CLOCK_SKEW_MAX`; cross-witness/reference comparison suppresses positive trusted time + Q3 when unavailable or beyond threshold. §12 L299 states the correlated-compromise residual. |
| NB-11 | `WITNESS_RETENTION_MIN` undefined duration | **PARTIAL** | INV-015 (L190) names `WITNESS_RETENTION_MIN` as a signed-policy quantity; §12 L300 records the default remains owner-open. Default not proposed in v0.3 (unlike break-glass caps). **PARTIAL** — defensible (default proposal belongs to 0B), but a proposed default would be a marginal improvement. Non-blocking. |
| NB-12 | §6 ADV-23 anchor/witness conflation | **CLOSED** | L121 ADV-23: "One witness operator, including a combined anchor function"; §3 L59 ASSUMPTION: anchor is a witness function. Aligned. |
| NB-13 | `CapabilityGrant`×ADV-01 cell carries unstated precondition | **CLOSED** | L144 row precondition column explicitly states "requires OS-authenticated process identity and harness membership not shared-UID/self-asserted"; L147 ASSUMPTION: † marks the cell; L147–149 give both the † and ‡ footnotes. |
| NB-14 | INV-058 test family omits CT | **CLOSED** | L233 INV-058 test column: FW, PV, CT, MP. |
| NB-15 | Appendix A B14 status readability | **CLOSED** | L349: "Reviewer 3 `minimax-m3` completed the chartered third-lineage review on 2026-08-25 with verdict **accept-with-changes**." |
**15/15 non-blocking closed or correctly deferred.** Two (NB-9, NB-11) are owner-open values where v0.3 supplied the proposed default(s) where applicable and marked the rest for 0B — exactly the right division of labor.
## Regression spot-checks (v0.2 → v0.3 must not regress)
- **INV-055's expanded quantity set (NB-B1):** Verified L230 lists all nine maxima and two minima; §9 L257 mirrors. The resistance partition (ADV-01,02,03,04,05,06,07,09,10,11,12,13,14,15,16,17,18,20,21,22,23,24,26,27,28,29,30,31,34) is unchanged from the v0.2 structure and the prose at L230 confirms re-derivation was performed. No regression.
- **§5 Step 3 / Step 5 wording:** Step 3 (L77) retains the termination-rule structure and adds the explicit "even when an exempt event lies inside … effect range" precedence; Step 5 (L79) is now explicit about Step-4-marked direct actions and Step-5-marked descendants. The mutual-revocation result (L85) and fork terminal state (L89) are unchanged in substance. No regression; the §5 walkthrough I did for v0.2 still passes.
- **§11 element 5 text (L286):** Now states both the recommended default and the explicit non-goal grounding ("explicitly because Plan 025 makes ingesting, retaining, or attesting patient data a non-goal"). The "Choosing (b)" sentence carries compliance-liability framing that resolves NB-6. The new closing sentence ("Until the owner selects (b), free text is forbidden and conditional INV-046/INV-048 retention branches are inactive") closes NB-B3 without regressing the immutability residual (which is still named in the same sentence). No regression.
## New defects introduced by v0.3
None blocking. One minor non-blocking observation:
- **NEW-NB-v3.1:** §12 L300 says "`WITNESS_RETENTION_MIN` is policy-bound but its default remains owner-open." This is correct but the asymmetry with §9 L268 (which *does* propose defaults for break-glass caps but not for witness retention) is mildly inconsistent — both are owner-open values, yet one is named with a proposed default and the other is not. Recommend proposing a default retention (e.g. 7 years for non-PHI development artifacts) in 0B. Non-blocking.
No other defects found. The matrix self-check at L236 is preserved; the coverage sweep at L242–253 is preserved; the adversary catalogue is unchanged (ADV-01–34 stable); the invariant IDs INV-001–059 are stable; the INV-053 resistance column now includes ADV-08 (which v0.2 omitted) — that is an addition, not a regression, and is consistent with HSM key custody's relationship to bootstrap-root.
## VERDICT
**Accept.** All three blocking items NB-B1, NB-B2, NB-B3 are genuinely closed in v0.3 with the cited text, and the regression spot-checks on §5 step 3/5, INV-055's expanded quantity set, and §11 element 5 show no degradation from v0.2. The fifteen non-blocking items are closed or correctly deferred. v0.3 is materially responsive to my v0.2 review.
**Statement for owner sign-off:** *The invariant registry (INV-001 through INV-059, 6×34 claim matrix, §5 temporal algorithm, §9 degraded-state rules) is **ready for owner sign-off** contingent on the owner explicitly resolving the eight marked `OPEN:` decisions in §1, §5, §9, §10, §11 (which is the correct posture for a 0A-1 trust-model artifact — the model supplies proposed defaults and normative structure; the owner supplies the deployment values).*
