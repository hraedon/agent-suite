# Phase 0B substrate and anchor bake-off rubric

Status: 0A-5 draft. This rubric defines measurements; it does not contain candidate scores or a substrate decision.

Normative inputs are Plan 025 v3.1 sections 3, 5, 6, and 8; the Phase 0A charter; and `TRUST-MODEL.md` sections 6, 8, and 9. `ADV-*` and `INV-*` identifiers below cite the stable v0.2 registry IDs, which remain unchanged in the concurrent v0.3 draft.

## 1. Freeze and use rules

1. Freeze this rubric at the 0A exit before collecting any scored 0B measurement. Record the frozen revision, date, reviewers, and owner approval in the 0B results.
2. Run the same mandatory workload, adversary scenarios, fixtures, scale profile, and evidence-capture procedure for A, B, and C. Candidate-specific setup is allowed but must be recorded.
3. Do not change a predicate, weight, workload, or scale after seeing candidate results. A necessary correction requires a new rubric revision, written rationale, and complete rerun of every affected measurement for all candidates.
4. A failed applicable security invariant is disqualifying, not a weighted deduction. Preserve the evidence and identify the failed `INV-*`; do not calculate a weighted total for that candidate.
5. Weighted scoring applies only to candidates that pass every applicable security predicate. Use the evidence anchors and scoring method in section 7; do not substitute an overall impression.
6. A time-box expiration is not a pass. Mark unfinished work `INCOMPLETE`. Incompleteness in the mandatory security core defined in section 3 is disqualifying; an extended operational row may remain `INCOMPLETE` without security disqualification but receives no favorable evidence or score.
7. No candidate is pre-scored. Candidate B is an operational-fit hypothesis whose premises must pass section 8 before environment-fit evidence may receive scoring weight. All three candidates remain security-unproven until measured.

`OPEN:` At the 0A exit, record the frozen git revision, freeze date, two concurring lineages, and owner approval.

## 2. Candidates

| ID | Candidate under test | Neutral test boundary |
|---|---|---|
| A | Hardened PostgreSQL used as the projection store, with a signed append-only log as source of truth and witnessed transparency-log checkpoints; signed segments may optionally be retained in WORM/object storage. | Measure the log, checkpoint, witness, PostgreSQL projection, optional WORM, verifier, backup, and restore procedures as one operated design. Optional WORM may add defense in depth but may not substitute for proofs or independent witnesses. |
| B | SQL Server 2022 Ledger tables with independently retained digest witnesses. | Measure Ledger, digest generation/distribution, independent witness retention, verifier, backup, and restore procedures as one operated design. Ledger is tamper-evident rather than forgery-preventing; its environment fit and security properties remain hypotheses. |
| C | immudb as a purpose-built immutable store with independent witnesses. | Measure immudb proofs/checkpoints, witness retention, verifier, projection integration where needed, backup, and restore procedures as one operated design. Product claims do not substitute for locally reproduced evidence. |

Each implementation must expose one language-neutral evidence package sufficient for local verification of the relevant inclusion, consistency, checkpoint, witness, authority, and recovery assertions. A centralized service verdict alone does not satisfy the workload (`INV-019` through `INV-023`).

Candidate A's use of the requirements vocabulary (cuts, signed log, checkpoints, witnesses, and proofs) confers no credit. A must produce the same measured evidence and pass the same predicates as B and C; matching vocabulary or asserting the target design is not measurement.

For candidate B, verification executed inside SQL Server satisfies independence only when an external verifier can reproduce the digest-chain result from independently retained digests without trusting the live instance's verdict. Record the external verifier command/version, independent digest source and custody, input/output digests, and a tampered-instance negative test; absence of that evidence fails the applicable local-verification predicate rather than pre-judging the product.

## 3. Time box and common controls

`ASSUMPTION:` Allocate at most **5 engineer-days per candidate**, including environment setup, mandatory workload, adversary injection, recovery, evidence packaging, and runbook execution: **15 candidate-days total**. With one executor and one independent observer working sequentially, the elapsed bound is **3 working weeks**; shared harness preparation and final analysis may consume **2 additional engineer-days**, for **17 engineer-days total**. The owner may reduce scope only by revising and re-freezing this rubric before measurement; the mandatory security predicates may not be waived.

**Mandatory security core.** The disqualifying core is: Append; Rotation; Revocation with effect range; Inclusion and consistency proof; Checkpoint and witness; Fork; Rollback; Restore security predicate; Projection rebuild; Concurrency/TOCTOU; Unanchored outage/recovery; Break-glass; and every section 5 adversary scenario. Representative read volume, cross-platform ergonomics, performance percentiles, licensing, staffing, and operational-cost measurements are extended rows: they may be `INCOMPLETE`, receive no favorable evidence, and make the corresponding criterion unscored, but do not by incompleteness alone assert an invariant failure.

`OPEN:` Before freeze, the owner must size the 5-day box against a dry run of this mandatory security core, record expected duration per core row, and either approve the box or revise/re-freeze it. Universal `INCOMPLETE` is not an acceptable bake-off result.

Use these common controls:

- Pin candidate product/version, OS, driver, verifier, schema/protocol, witness policy, and deployment configuration before each run.
- Use identical canonical event bytes, authority transitions, revocation ranges, logical history, witness topology, retained prior cuts, and read query mix.
- Use equivalent hardware/resource limits or record and normalize every difference. Do not compare raw throughput across materially different resources without a repeated equal-resource run.
- Run Windows and Linux client/driver exercises for every candidate. The substrate server may use its supported target OS, but verifier and operator workflows must be exercised from both target client platforms.
- Capture commands, configuration digests, timestamps, structured verifier output, proof/checkpoint bytes or digests, witness observations, resource telemetry, and operator elapsed time. Exclude secret values.
- Begin from a separately authenticated genesis policy (`INV-009`) and a signed witness/authority policy (`INV-016`, `INV-036`). Retain a verifier-pinned pre-attack checkpoint outside the writer, database, DBA, and tested witness failure domains.
- Acquire the root/genesis/witness-roster pin through an operator-independent authenticated pin channel and record publisher, channel authentication, digest, predecessor/version, and custody. Run first contact once with that pin and once without it; the no-pin run may establish internal consistency only and must issue no trusted Q2/Q3/claim (`INV-052`).
- Treat malformed, stale, absent, warning, partial, halted, or contradictory evidence as denial rather than PASS (`INV-003`, `INV-006`, `INV-022`).

`OPEN:` Before 0B starts, the owner must approve the event count, payload-size distribution, read concurrency, read/query mix, checkpoint cadence, witness quorum, resource envelope, recovery-point/recovery-time targets, `PERMISSION_NOW_MAX_STALENESS`, and each stricter per-action cut-observation floor used as the representative frozen scale profile. Every Rotation result records the applicable frozen floor numerically.

## 4. Mandatory falsification workload

The purpose is to falsify security or operability with the minimum common implementation, not to build three production prototypes. Run every row against each candidate within its time box.

| Workload | Procedure | Measurement to record | Pass/fail predicate |
|---|---|---|---|
| Append | Append canonical signed events, including duplicate ids/nonces, a predecessor mismatch, a mutable-row mismatch, and an event with absent or unknown authentication. For one valid submission capture the signed acknowledgement, then withhold merge past its promised deadline; separately withhold acknowledgement entirely. Include normal and burst rates. | Accepted/denied counts and reasons; acknowledgement fields/signature; promised position/receipt/policy/deadline; witnessed inclusion result; censorship incident/publication; committed root; latency/throughput/resources. | **PASS** only if valid events occupy one unique authenticated order; invalid inputs deny; an acknowledgement binds the submission and maximum merge delay; missed inclusion deadline stops dependent action and publishes censorship evidence through the independent channel (`INV-001` through `INV-005`, `INV-022`, `INV-051`). Never-acknowledged censorship is recorded as a residual, not silently called prevented. |
| Rotation | Rotate an authorized signing key, append before and after rotation, then attempt a new action with the superseded key and verify historical events at later cuts. Test cuts just inside and just outside the frozen action floor. | Transition positions/cuts; witnessed observation age; applicable frozen action floor; old/new key outcomes; cache invalidation; operator steps/time; verifier Q1/Q2 fields. | **PASS** only if authority is evaluated at the authenticated prior position, valid history retains Q1, and the old key cannot perform a new action after loss of permission (`INV-008`, `INV-010` through `INV-012`, `INV-020`). Acceptance from a cut whose witnessed observation age exceeds the applicable action floor is **FAIL**. |
| Revocation with effect range | At position `r`, issue an authorized dual-control revocation whose effect starts at earlier position `e` and has a finite test end `t`; verify events before, within, and after the range at cuts before and after `r`. Attempt malformed, unauthorized, timestamp-based, self-covering, and out-of-policy ranges. | Issuance decision; bound fields/positions; Q1/Q2 before and after knowledge cut; affected event count; denial reasons; replay time. | **PASS** only if the authorized range deterministically changes Q2 for `e..t` after knowledge cut `r`, preserves Q1/history, leaves positions outside the range unchanged, and denies invalid or unavailable policy (`INV-008`, `INV-012`, `INV-013`, `INV-020`, `INV-028`). Any caller-time authority or unauthorized retroactive effect is **FAIL**. |
| Inclusion and consistency proof | Request proofs for first/middle/last events and growth between retained cuts. Tamper with leaf, path, size, root, range count, filter, schema, and proof bytes; request partial/filtered evidence. | Proof size; generation and local verification latency; malformed-input resource use; result fields and completeness scope. | **PASS** only if valid proofs verify locally and every tampered, ambiguous, oversized, partial-as-complete, or inconsistent proof denies within limits (`INV-003`, `INV-005` through `INV-007`, `INV-014`, `INV-021`, `INV-045`). |
| Checkpoint and witness | Produce signed chained checkpoints at the fixed cadence, distribute them through the candidate design, obtain the pinned witness quorum, and verify from independently retained prior cuts. Withhold one witness and submit a false/stale observation from one witness. | End-to-end checkpoint age; publication/witness latency; quorum result; retained locations/operators; bytes and signatures; alert/refusal latency. | **PASS** only if checkpoints bind required identity/state, witnesses retain signed monotonic observations, quorum respects independent failure domains, and stale/unavailable quorum cannot produce fresh positive Q3 (`INV-014` through `INV-018`). One operator satisfying a multi-witness quorum is **FAIL**. |
| Fork | Present two histories extending one accepted cut to different clients/witnesses; attempt split-view checkpoint publication and later convergence without incident handling. | Which parties observed each cut; detection channel/latency; verifier output; alerts; operator response and retained evidence. | **PASS** only if inconsistent growth or equivocation is rejected and surfaced by local retained cuts or required witnesses; it may not be silently repaired or aggregated to PASS (`INV-005`, `INV-014` through `INV-018`, `INV-021`, `INV-022`). An undetected fork below the declared non-resisted quorum is **FAIL**. |
| Rollback: old DB plus old checkpoint | After newer checkpoints are independently retained, replace the live database and its locally available checkpoint with a mutually consistent older pair. Attempt reads, appends, and positive Q3 claims before recovery. | Old/new cuts; first detection point/latency; denied operations; alert; recovery steps; source of expected cut. | **PASS** only if the verifier learns the expected cut from pinned/local independent state, rejects stale substitution, and withholds fresh positive Q3 and authority-bearing actions until consistent recovery (`INV-011`, `INV-017`, `INV-018`, `INV-022`, `INV-038`, `INV-049`). Acceptance of the self-consistent old pair is **FAIL**. |
| Restore | Restore a backup to clean infrastructure after advancing beyond the backup cut. Verify artifacts/profile, reconcile from an external retained cut, replay authority, reconcile witnesses, and resume operation. Also attempt to resume before all checks complete. | RPO/RTO; operator touch time; steps and errors; restored and recovered cuts; proof/replay duration; first allowed write; witness status. | **PASS** only if premature operation denies and restored service is accepted after artifact/profile verification, full consistency from retained external state, authority replay, witness reconciliation, and projection equivalence (`INV-017`, `INV-018`, `INV-035` through `INV-038`, `INV-049`). |
| Projection rebuild | Corrupt/delete mutable projection rows, then rebuild from authenticated source evidence at a fixed cut/policy. Compare to the pre-corruption canonical projection and repeat after revocation. | Row/digest differences; rebuild duration/throughput; resource peak; cut/policy inputs; mismatch/refusal output. | **PASS** only if mutable fields cannot grant authority, a fresh rebuild is deterministic and equal at the same cut/policy, and any source inconsistency denies (`INV-004`, `INV-007`, `INV-012`, `INV-038`, `INV-039`). |
| Concurrency / TOCTOU | At deterministic barriers, interleave cancel/approve/commit, key rotation/authorization, checkpoint publication, policy check/credential issuance, and expected-state lifecycle updates; repeat each schedule with stale caches and losing writers. | Schedule/seed; lock order; state/version read before and after lock; CAS rows affected; retries/denials; committed events/cuts. | **PASS** only if every authority-affecting transition re-reads after lock, enforces expected state/version, binds check to commit, and stale/check-use schedules deny or retry without resurrection or duplicate authority (`INV-026`, `INV-027`, `INV-054`; `CT`). |
| Unanchored outage and recovery | Begin anchored, make the anchor unavailable, attempt bounded degraded capture and positive Q3, exceed the frozen unanchored maximum, restore anchor, reconcile, and query claims intersecting the range. | Last anchored/first reanchored positions; exact range/times; denied claims; marker bytes/signature; reconciliation proof; post-recovery claim fields. | **PASS** only if outage suppresses fresh Q3, operation never exceeds the signed hard window, recovery appends the chain-committed marker, and every intersecting claim permanently states `unanchored=true` (`INV-018`, `INV-055`, `INV-058`). |
| Break-glass | Invoke with the required distinct approvers; perform one permitted action; attempt prohibited action, renewal, incident-id reset, cumulative per-incident and cross-incident cap overruns; let authority expire; reconcile; append closure; attempt ordinary operation before and after closure. | Approver identities/custody; pre-issued sequence and predecessor; incident/scope/start/expiry/action records; cumulative counters; denials; reconciliation and closure; first ordinary action. | **PASS** only if records are pre-numbered and chained, caps are cumulative across invocations/incident ids, prohibited/expired actions deny, extension requires the distinct non-emergency path, and ordinary operation resumes only after complete reconciliation and closure (`INV-038`, `INV-049`, `INV-055`). |
| Representative read volume | Run the frozen query mix at the frozen volume/concurrency in healthy state, stale-checkpoint state, witness outage, and post-rotation/revocation state. Verify bounded proofs locally on sampled and policy-required reads. | p50/p95/p99 latency; throughput; error/deny rate; CPU/memory/network; proof/cache hit rates; checkpoint age; stale-result count; Windows/Linux driver behavior. | **PASS** only if every result remains bound to evidence/root/policy/cut, cache invalidation and freshness rules hold, stale or unavailable Q3 is represented honestly, and no partial/unknown result becomes PASS (`INV-007`, `INV-011`, `INV-012`, `INV-018` through `INV-023`, `INV-045`, `INV-049`). Performance has no security override. |

## 5. Mandatory adversary scenarios

Run these scenarios against every candidate in addition to the workload injections above. "Detection" means a verifier or required witness produces a durable, operator-visible fault from independently retained/pinned state. A database's or writer's self-report is not independent detection. The `must-PASS` column contains only registry invariants that claim resistance to that adversary; `expected residual` contains cited invariants whose registry explicitly does not resist it.

| Adversary | Injection | Expected detection/refusal behavior | Must-PASS (registry resists) | Expected residual (registry does not resist) |
|---|---|---|---|---|
| `ADV-03` compromised provenance writer | Omit, reorder, duplicate, fork, delay/fabricate rows/checkpoints; return false append success. | Proof/scope/fork failures deny; acknowledged omission triggers deadline incident. Correctly authorized malicious append and never-acknowledged censorship remain residuals. | `INV-004`, `INV-006`, `INV-007`, `INV-016`-`INV-018`, `INV-021`, `INV-022`, `INV-051` | `INV-005`, `INV-014` |
| `ADV-12` DB writer | Mutate rows/projections, race readers, create unsigned pending/PASS state. | Mismatch denies; local verification and rebuild prevail. | `INV-004`-`INV-007`, `INV-022`, `INV-024`, `INV-038`, `INV-039`, `INV-054` | none |
| `ADV-13` DB administrator | Disable DB controls, rewrite/fork, restore old backup, present stale state. | External cuts expose rewrite/rollback; verified reconciliation precedes operation. | `INV-005`, `INV-006`, `INV-014`, `INV-016`-`INV-018`, `INV-022`, `INV-038`, `INV-049` | none |
| `ADV-14` writer-zone host root | Replace writer, read writer keys, alter clock/network/storage, issue malicious checkpoints, rollback/fork. | Independent history below quorum remains detectable; do not claim writer forgery prevention. | `INV-010`, `INV-016`-`INV-018`, `INV-021`, `INV-051` | `INV-005`, `INV-014` |
| `ADV-23` one anchor/witness operator | Withhold/erase/sign false observation and publish one fork. | One witness cannot satisfy quorum; conflict/absence denies and alerts. | `INV-016`-`INV-018`, `INV-021`, `INV-022`, `INV-049` | `INV-015` |
| `ADV-26` writer + DBA collusion | Valid writer plus arbitrary DB rewrite/omission/rollback/fork. | Independent witnesses/cuts detect below quorum; malicious authorized appends remain residual. | `INV-004`, `INV-006`, `INV-016`-`INV-018`, `INV-021`, `INV-038`, `INV-051` | `INV-005`, `INV-014` |
| `ADV-27` DBA + one witness operator | Fork/restore DB and have one witness endorse/suppress. | Below-quorum collusion cannot yield fresh Q3. | `INV-014`, `INV-016`-`INV-018`, `INV-022` | `INV-015`, `INV-038` |
| `ADV-34` emergency approver collusion | Required emergency approvers coordinate to extend, equivocate, reset incident ids, or perform prohibited actions. | Enforcing boundaries reject cap/floor/record violations; abuse inside granted emergency scope is reported as residual. | `INV-055` | `INV-049` |

For each scenario, record compromised credentials/zones, exact steps, cuts and digests before/after, expected and observed result, time to detection/refusal, alerts, operator action, and residual capability. Do not claim resistance when the scenario controls a full configured witness quorum; the trust model explicitly treats writer/DBA plus witness-quorum compromise as a residual.

## 6. Operational requirements per option

Populate every cell with measured facts, named roles, dependencies, and runbook references. `UNKNOWN`, `OPEN`, or undocumented manual knowledge does not silently receive a favorable score.

| Candidate | Witness count / operators / monitoring | Anchor-key custody / rotation / recovery | Checkpoint distribution channel and its trust | Substrate operations: licensing, skills, verify runbook, Windows and Linux driver ergonomics | Fail-closed recovery / break-glass | Backup / restore with re-verification | Version-skew and policy governance |
|---|---|---|---|---|---|---|---|
| A | Record configured quorum, independent organizations/failure domains, retained state, alert ownership, coverage, cadence, and retention. | Record key type/location, HSM/FIPS support, custodians, issuance, rotation overlap, revocation, escrow/recovery, and dual control. | Record publisher, transport/repository, authentication, availability, client discovery, pinning, replay/staleness defense, and which operators can alter it. | Record PostgreSQL/log/WORM licenses and support; required DBA/SRE/crypto skills; install, patch, capacity, HA, proof-verification and incident runbooks; execute client/driver setup and local verification on Windows and Linux. | Record exact denied operations during anchor/witness/verifier outage, freshness behavior, recovery authority, emergency scope/expiry, dual control, audit/reconciliation, and prohibited bypasses (`INV-018`, `INV-049`). | Execute backup and clean-host restore; record RPO/RTO, external retained cuts, artifact/profile verification, replay, witness reconciliation, projection equivalence, and first safe write (`INV-038`). | Record compatibility matrix and behavior for mixed server/client/proof/schema/policy/witness versions; unknown/downgrade refusal; signed policy ownership, review, rollout, rollback, and evidence retention. |
| B | Record configured quorum, independent organizations/failure domains outside SQL Server/DBA control, retained digests, alert ownership, coverage, cadence, and retention. | Record Ledger digest/anchor signing key type/location, HSM/FIPS support, custodians, rotation, revocation, escrow/recovery, and separation from DBA credentials. | Record digest publisher, transport/repository, authentication, availability, client discovery, pinning, replay/staleness defense, and whether DBA/SQL infrastructure can alter both data and expected digest. | Record exact SQL Server 2022 edition/features, licenses/support, Windows/Linux server constraints, DBA/security skills, HA/patch/capacity/digest/verification runbooks; execute client/driver setup and independent verification on Windows and Linux. | Record exact denied operations during digest/witness/verifier outage, freshness behavior, recovery authority, emergency scope/expiry, dual control, audit/reconciliation, and prohibited bypasses (`INV-018`, `INV-049`). | Execute SQL backup and clean-host/HA restore; record RPO/RTO, external digest cuts, artifact/profile verification, Ledger verification, authority replay, witness reconciliation, projection equivalence, and first safe write (`INV-038`). | Record compatibility matrix and behavior for mixed SQL cumulative updates, drivers, verifier/proof/digest formats, policy and witnesses; unknown/downgrade refusal; signed policy/digest governance, rollout, rollback, and retention. |
| C | Record configured quorum, independent organizations/failure domains, retained state, alert ownership, coverage, cadence, and retention. | Record immudb/checkpoint/anchor key type/location, HSM/FIPS support, custodians, issuance, rotation, revocation, escrow/recovery, and dual control. | Record publisher, transport/repository, authentication, availability, client discovery, pinning, replay/staleness defense, and which operators can alter it. | Record immudb licenses/support; product-specific DBA/SRE/crypto skills; install, patch, capacity, HA, proof-verification and incident runbooks; execute client/driver setup and local verification on Windows and Linux. | Record exact denied operations during witness/verifier outage, freshness behavior, recovery authority, emergency scope/expiry, dual control, audit/reconciliation, and prohibited bypasses (`INV-018`, `INV-049`). | Execute backup and clean-host restore; record RPO/RTO, external retained cuts, artifact/profile verification, cryptographic consistency, authority replay, witness reconciliation, projection equivalence, and first safe write (`INV-038`). | Record compatibility matrix and behavior for mixed server/client/proof/schema/policy/witness versions; unknown/downgrade refusal; signed policy ownership, review, rollout, rollback, and evidence retention. |

## 7. Eligibility and scoring

### 7.1 Disqualifying invariant gate

A candidate is eligible for weighted scoring only when all mandatory workload and adversary rows pass every cited applicable invariant. Any of the following disqualifies it:

- A demonstrated failure of an invariant in that adversary row's **must-PASS** set. A demonstrated non-resistance already conceded in its **expected residual** set is recorded as a residual and never disqualifies by itself.
- Inability to complete a mandatory security test reproducibly inside the time box.
- A witness design that does not place the required quorum in failure domains independent of the writer and DBA (`INV-016`).
- Rollback to an old database plus old checkpoint producing a fresh positive Q3 result (`INV-017`, `INV-018`).
- Restore or break-glass resuming ordinary operation without the required re-verification and reconciliation (`INV-038`, `INV-049`).
- Any rollback, runtime configuration, or emergency record lowering a monotonic security floor (`INV-055`), or any component/claim below a pinned minimum version obtaining PASS (`INV-057`).
- Dependence on a centralized unauthenticated/self-authenticated verdict where local proof verification is required (`INV-019` through `INV-023`).

Security passes are not points and do not enter the weighted total.

### 7.2 Weighted criteria

For an eligible candidate, score each criterion from 0 to 5 using the evidence anchors below. Weighted points are `criterion weight * score / 5`, totaling 100 maximum. Publish each raw score, evidence reference, and calculation. Ties remain ties unless 0C defines and freezes a tie-break rule before opening results.

| Criterion | Weight | Evidence and anchors |
|---|---:|---|
| Operational sustainability after the independence gate | 25 | Score only candidates that passed `INV-016`. Measure staffing coverage, monitoring/alert ownership, checkpoint cadence/retention, routine touch time, and rehearsed verification/incident runbooks. `0` = post-gate operation is not repeatable; `1` = heroic/manual and unowned despite compliant topology; `3` = named roles/repeatable runbooks with material gaps; `5` = monitored, rehearsed, sustainable operation under target staffing. Independence itself earns no points. |
| Recovery, degraded state, and break-glass operability | 20 | Measured RPO/RTO; restore success; external-cut reconciliation; projection rebuild; outage refusal; dual-control emergency flow and audit closure. `0` = unsafe/incomplete recovery; `1` = manual recovery cannot demonstrate consistency; `3` = successful recovery with notable manual risk or target miss; `5` = repeatable target-meeting fail-closed recovery with complete re-verification. |
| Representative read and append performance | 15 | Equal-resource p50/p95/p99 latency, throughput, resource/storage growth, proof cost, checkpoint lag, and behavior under the frozen healthy/degraded query mix. `0` = workload cannot complete; `1` = unusable or unbounded; `3` = meets minimum targets with limited margin; `5` = meets all targets with documented capacity margin and bounded degradation. |
| Platform integration and driver ergonomics | 15 | Reproduced install, authentication, connection, proof-verification, diagnostics, and upgrade workflows on both Windows and Linux clients; target server/HA integration. `0` = a required platform cannot operate; `1` = unsupported/custom fragile path; `3` = supported path with material friction; `5` = supported, automated, diagnosable path on both platforms. |
| Deployment, maintenance, and implementation complexity | 10 | Component/key count; custom security-sensitive code; install/patch/upgrade steps; HA topology; conformance surface; failure modes; time to a reproducible environment. `0` = non-repeatable or unmaintainable; `1` = large bespoke TCB/runbook burden; `3` = manageable documented complexity; `5` = minimal repeatable design with small custom TCB and automated conformance. |
| Licensing, supportability, and lifecycle cost | 10 | Target-valid license/edition/support terms; infrastructure and support dependencies; training/on-call/HA/witness costs; product lifecycle. `0` = unavailable or prohibited; `1` = unbounded/unfunded; `3` = feasible with material cost or dependency; `5` = fully available, budgetable, supported, and lifecycle-aligned. |
| Version-skew and policy governance | 5 | Mixed-version tests; fail-closed schema/proof/policy behavior; signed rollout/rollback; ownership and evidence retention. `0` = unsafe skew or unsigned policy; `1` = undocumented/manual; `3` = tested supported matrix with gaps; `5` = automated compatibility enforcement and governed signed transitions. |
| **Total** | **100** | No weighted score is calculated unless section 7.1 passes. |

The measured operational-requirements table is the primary scoring evidence. A vendor feature list, architectural preference, or unvalidated target-environment assumption is not measurement.

The environment-fit criteria are **Operational sustainability after the independence gate**, **Recovery/degraded-state/break-glass operability**, **Platform integration and driver ergonomics**, and **Licensing/supportability/lifecycle cost**. While an applicable environment premise remains `OPEN`, that criterion is `UNSCORED` and the candidate is not eligible for a weighted total; no neutral or favorable placeholder is imputed.

Invariants not otherwise cited are outside this substrate bake-off for stated reasons: `INV-053` key custody is a deployment-profile/HSM and per-key-owner control (capture capabilities in section 6, but prove it at profile qualification); `INV-056` concerns issuance of claim types with unselected application policy; `INV-059` concerns exact-subject human/agent review admission at app/gate/signer boundaries. They receive no implied PASS from substrate results.

## 8. Candidate B premise validation

Complete this checklist for the actual owner/target environment before candidate B's environment-fit evidence receives scoring weight. Each item remains open until supported by an owner statement, inventory, contract/license record, staffing commitment, or executed operational evidence.

- `OPEN:` Confirm SQL Server 2022 is permitted and available in the target environment in an edition and licensing model that includes every Ledger, HA, backup, verification, and support feature used by the candidate design. Name the owner and attach the authoritative license/edition evidence.
- `OPEN:` Confirm named DBA capability and coverage for Ledger configuration, security boundaries, patching, performance, incident response, digest operations, and adversarial verification. Record staffing depth, on-call ownership, and training gaps.
- `OPEN:` Confirm the target environment has, or commits to establish and rehearse, the required SQL Server HA and clean-host backup/restore practice. Attach measured RPO/RTO and post-restore Ledger re-verification evidence.
- `OPEN:` Confirm operational support for Ledger digest generation, signing/authentication where applicable, publication cadence, retention, monitoring, verification, incident handling, and recovery. Name the digest-management owner and verify the tooling/runbook rather than assuming ordinary DBA operations cover it.
- `OPEN:` Confirm the required witness quorum is operated, monitored, and retains expected digests **outside the SQL Server, SQL host/cluster, backup platform, DBA credentials, DBA management plane, DBA reporting chain/failure domain, directory/IdP administrator (`ADV-30`), and secret-backend administrator (`ADV-31`)**. Name operators, custody, infrastructure, distribution channels, and collusion assumptions; one DBA-controlled digest repository does not satisfy `INV-016`.

Failure of licensing availability makes B operationally ineligible for the target environment. Failure of witness independence is a security disqualification under `INV-016`, not a weighted operational deduction. Other unresolved premises remain `OPEN`; the named environment-fit criteria are `UNSCORED`, and B has no weighted total until they are evidenced.

## 9. Language and stack criteria for 0C

These are decision criteria only. 0A does not select Python, .NET, a repository/process layout, an SDK/sidecar form, or another implementation stack. Apply them in 0C after the substrate and operational evidence is available, while preserving one language-neutral protocol, one authoritative implementation, and a conformance suite.

| Criterion | Evidence required for the 0C decision |
|---|---|
| Portability | Supported target OS/architecture/runtime matrix; deterministic canonicalization and cryptographic behavior across platforms; local/offline verification options; stable FFI/wire boundary; reproducible builds and conformance results; migration and long-term runtime support. |
| Windows operations | Service installation and identity model; AD/integrated-auth options where applicable; certificate/key stores; patching, logging, eventing, monitoring, crash diagnostics, least-privilege operation, packaging, and PowerShell/operator ergonomics on the target Windows estate. |
| FIPS/HSM integration | Availability and support status of required FIPS-validated cryptographic modules in the actual deployment mode; HSM/KMS/CNG/PKCS#11 integration; key non-exportability, purpose separation, rotation/recovery, algorithm constraints, testability, and failure behavior. "FIPS-capable" without module/version/mode evidence is insufficient. |
| Deployment | Artifact signing and verification; dependency and supply-chain footprint; offline/on-prem/hybrid deployment; service/process topology; upgrade/rollback; HA; configuration and secret handling; observability; installer and release provenance; ability to keep writer, verifier, and witness trust zones separate. |
| Staffing | Existing Windows, SQL, PostgreSQL, immudb, Python, .NET, cryptography, SRE, and incident-response capability; hiring/training/on-call needs; bus factor; ecosystem support; maintainability of the authoritative verifier and conformance suite over the planned lifecycle. |

`OPEN:` In 0C, name the decision owner, minimum acceptable evidence for each criterion, and whether any target-environment constraint is mandatory rather than weighted. Do not infer those constraints from candidate B's unvalidated premise.

## 10. 0B result record

For each candidate, the result must contain:

- Frozen rubric revision and common-fixture/configuration digests.
- Candidate versions, topology, resources, licenses, operators, trust/failure domains, and deviations.
- One evidence reference and verdict for every section 4 workload row and section 5 adversary row.
- An invariant-gate result listing each tested `INV-*` as `PASS`, `FAIL`, or `INCOMPLETE`, with reproduction evidence.
- The completed operational-requirements row and candidate B premise checklist where applicable.
- A symmetric premise/evidence ledger for A, B, and C. Each asserted dependency (including A's independent witness operator and C's licensing/support/staffing) is `EVIDENCED`, `FAILED`, or `OPEN`; `OPEN` supplies no favorable score. B additionally completes its chartered formal checklist.
- Weighted criterion evidence and arithmetic only if the invariant gate passes.
- Explicit residuals, especially malicious valid appends, full witness-quorum compromise, bootstrap-policy compromise, and availability loss.
- Time consumed, unfinished work, and every owner decision or assumption used.

The 0B record supplies evidence to 0C. It does not change this frozen rubric and does not itself select the language/stack.
